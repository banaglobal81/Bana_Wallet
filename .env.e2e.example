# Riobx E2E credentials — copy to `.env.e2e` (gitignored) and fill in.
# Used only by the Playwright suite (playwright.config.ts loads this file).
RIOBX_EMAIL="test102@test.com"
RIOBX_PASSWORD="1234"

# Optional: override the target host (defaults to https://riobx.com).
# RIOBX_BASE_URL="https://riobx.com"
