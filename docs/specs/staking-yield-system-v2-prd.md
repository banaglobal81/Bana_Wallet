# PRD — 스테이킹 수익 시스템 v2 (Staking Yield System v2)

> 작성: `pm` · 2026-08-10
> **개정 01: 2026-08-10 — 마스터의 §5.1 질의서 답변 반영.** 변경 범위: §2.1(신규),
> §4.3(C-10/C-11 추가), §4.4, §8.7(신규), §9(G-0′ → G-0″), §10, §11.3/§11.6,
> §13(전면 재구성), §13.1(질의서 압축), §14, §16. **게이트는 해제되지 않았다** — 차단 항목이
> 7개에서 4개로 줄고, 그중 하나가 파트너 질의가 아니라 **마스터의 결정 사항**임이 드러났다.
> 대체/흡수 대상: `docs/specs/staking-payout-rail-prd.md` §7(Track 2), `docs/specs/deep-core-04-yield-linkage-GATED.md`
> 유효 유지: `docs/specs/staking-payout-rail-prd.md` §6(Track 1) — **v2의 선행조건이며 이 문서가 대체하지 않는다**
> 게이트 갱신: `docs/specs/deep-core-00-overview-and-gate.md` §6.2 G-1/G-2/G-3 → 본 문서 §9로 재정의
>
> **이 문서는 구현 지시서가 아니다.** §13의 사람 승인 항목이 해소되기 전에는 어떤 에이전트도
> 스키마 마이그레이션·정산 로직·지급 코드에 착수해서는 안 된다. 화면 설계(§11)와 스키마
> **설계 검토**는 승인 전에도 진행 가능하나, 마이그레이션 실행은 불가하다.

---

## 0. 요약

마스터가 방향을 전환했다: 기존 이자 시스템을 고치는 것이 아니라 **다시 만든다**. 그리고
DEEP CORE의 성장이 "이자 위에 얹는 보너스"가 아니라 **수익률 구조 그 자체의 일부**가 되어야 한다.

이 문서의 다섯 가지 핵심 결정:

| # | 결정 | 요지 |
|---|------|------|
| **D-1** | **계약 수익 밴드(Contracted Yield Band)** 모델 채택 | 체결 시점에 `기준 일이율(보장)` **과** `최대 가산율`을 **함께** 스냅샷한다. 게임 진행도는 그 밴드 **안에서의 위치**만 결정한다. 계약 불변성은 "이율을 안 건드린다"가 아니라 **"밴드를 안 건드린다"** 로 재정의된다 |
| **D-2** | 지급 레일은 **명시적 클레임(Option C) 유지** | 재구축이 이 결론을 뒤집지 않는다. 오히려 밴드 모델은 지급 스트림을 **하나로 통합**해 클레임의 이점을 키운다 |
| **D-3** | 전환은 **통합 경로(밴드폭 0)** | 기존 포지션 = `최대 가산율 0`인 v2 포지션. 정산 엔진을 두 벌 운영하지 않는다. 자금 마이그레이션 없음, 메타데이터 백필만 |
| **D-4** | 그랜트 락은 **구조적으로 차단** | `fundingSource` 명시 필드를 도입해 "실입금되지 않은 원금"이 출금 락 산식에 들어갈 수 없게 만든다. 운영 규칙이 아니라 타입으로 막는다 |
| **D-5** | 상한 체계를 **지급 절단 → 인수 통제**로 전환 | 계약된 금액은 자를 수 없다. 자를 수 있는 것은 **얼마나 계약할 것인가**뿐이다. 04 문서의 4중 상한 중 2개가 이 원리로 재배치된다 |

**한 문장 요약:** 게임 보너스를 "언제든 끌 수 있는 재량적 부가금"에서 **"체결 시점에 상한과
하한이 모두 공시되고, 최대 부채가 계산 가능하며, 사전 충당 가능한 계약 요소"** 로 바꾼다.

---

## 1. 재구축이 흡수하는 기존 문제

`staking-payout-rail-prd.md`가 제기한 문제들이 v2에서 어떻게 처리되는지 **명시적으로** 매핑한다.
"재설계했으니 자연히 해결된다"는 서술은 금지한다 — 각 항목의 처리 방식을 지정한다.

| 기존 문제 | 출처 | v2에서의 처리 |
|-----------|------|---------------|
| (a) 이자가 사용자 출금 가능 잔고가 되는 경로 없음 | PRD §1 | **§4 클레임 레일**로 해결. v2의 유일한 지급 경로 |
| (b) UI가 이를 "Paid to date"로 표시(허위 표시) | PRD C5 | **Track 1은 v2를 기다리지 않는다.** 즉시 정정하고, v2 화면(§11)은 처음부터 3개 수치 분리 구조로 설계 |
| (c) 미충당 부채 규모 미상 | PRD B-5 | Track 1 R-3(관리자 대사 리포트)이 선행. **부채 규모를 모르는 상태에서 v2 활성화 불가**(§13 H-5) |
| (d) 그랜트 락 의심 | PRD F-B / B-4 | **§6에서 구조적으로 재설계.** 다만 "오늘 이미 피해가 있었는가"는 여전히 운영 확인 필요(B-4) |
| (e) `paidInterest` 전량 재계산(F-C) | PRD F-C | **§5.3에서 강제로 소멸.** 일별 금액이 변동하므로 `perDay × dueDays` 재계산이 수학적으로 성립하지 않게 된다 |
| (f) 갱신 시 복리 여부(B-3) | PRD F-E / B-3 | **답변 확보(§2 N-1): 복리 아님.** `stakingRenew.ts`가 작성 완료되어 확인 가능해졌다 |
| (g) 출금 라우트의 `locked > 0` 분기 함정(F-D) | PRD F-D | Option C를 유지하므로 **여전히 회피됨.** v2도 출금 가용액 산식을 바꾸지 않는다 |
| (h) `ReferralBonusPayout` 동일 결함 | PRD C7 / D-6 | 동일 방침 유지: 레일 형태만 맞추고 **활성화하지 않는다.** 단 §3.6의 기준액 규칙이 추가된다 |

### 1.1 기존 부채 처리 방침 (마스터가 명시적으로 물은 항목)

**결론: 전액 인정한다. 탕감하지 않는다. 별도 정산 프로세스를 만들지 않는다. 백필 작업도 하지 않는다.**

근거와 메커니즘:

1. **탕감(clawback) 불가.** 발생한 이자는 약정에 따른 정당한 채무다. 지급 수단이 없다는 것은
   BANA의 문제이지 사용자의 문제가 아니다(기존 D-1 승계). 재구축은 채무를 없애는 사유가 아니다.
2. **별도 정산 불필요.** §5의 전환 전략이 `StakingPayout`을 **v2의 원장으로 그대로 승계**하므로,
   기존 행이 곧 v2의 미청구 잔액이 된다. 레일 가동 첫날부터 **그냥 클레임 가능해진다.**
3. **백필 작업 불필요.** 옮길 돈이 없다(자금 이동이 아니라 원장 승계다). 필요한 것은
   `baseAmount = amount`, `bonusAmount = "0"` 같은 **메타데이터 컬럼 백필**뿐이며, 이는 금액을
   바꾸지 않는 순수 스키마 작업이다.
4. **단, 선충당 조건은 그대로 승계된다.** 레일 개방 시점에 트레저리는 **미청구 총액의 100%**를
   즉시 지급 가능해야 한다. 부분 충당 상태로 여는 것은 금지한다(먼저 누른 사람만 받는 상황은
   지금보다 나쁘다). → §13 H-5/B-6.

> **이 방침의 핵심은 "재구축이 부채를 리셋할 기회가 아니다"라는 것이다.** 시스템을 새로
> 만든다는 사실이 이미 발생한 채무의 성격을 바꾸지 않는다. 재구축은 **앞으로 발생할 부채의
> 구조**를 바꿀 뿐이다.

---

## 2. 코드 재검증 결과 (2026-08-10)

`docs/patterns/pm.md`("승인의 근거가 되는 주장은 코드로 검증한다")에 따라 직접 확인했다.
기존 PRD의 C1~C7·F-A~F-E는 재확인 결과 여전히 유효하다. **아래는 이번 패스에서 새로 확정된 사실이다.**

| ID | 사실 | 근거 | 영향 |
|----|------|------|------|
| **N-1** | **자동 갱신은 복리가 아니다.** 승계 포지션의 원금은 `principal` 단독이며, 발생 이자는 포함되지 않는다 | `web/src/lib/stakingRenew.ts:201-202` — "Eligible — restake the principal. Interest already earned is NOT included" | **기존 PRD의 B-3이 해소되었다.** 미충당 부채가 복리로 증식하지 않으며, F-B와 같은 2차 피해도 없다. 부채 증가는 선형이다 |
| **N-2** | 정산은 `paidInterest = perDay × dueDays`로 **전량 덮어쓴다** | `stakingSettle.ts:74,83,128,131` | F-C 재확인. v2에서 이 산식은 성립 불가(§5.3) |
| **N-3** | `lockedPrincipalByCoin`은 ACTIVE 포지션 전부의 원금을 합산하며 `grantedByAdminId`를 구분하지 않는다 | `web/src/lib/staking.ts:69-79` | F-B 재확인. §6의 근거 |
| **N-4** | 출금 라우트의 락 검증은 `locked.gt(0)` 분기 안에 있고, 락이 있을 때만 허브 잔고를 조회해 **fail-closed** 한다 | `web/src/app/api/nia/withdrawals/route.ts:207-212` | F-D 재확인. v2도 이 산식을 건드리지 않으므로 함정 회피 |
| **N-5** | `StakingProduct.minAmount`는 여전히 `String?`(nullable) | `schema.prisma:240` | 00 §6.3 M-1(먼지 포지션 farming)의 전제가 여전히 성립 → v2에서 **밴드 상품에 한해 강제 non-null**(§7.3) |
| **N-6** | 스테이킹 가능 자산은 여전히 BANA 하나뿐이다 | `admin/staking/products/route.ts:73`, `admin/staking/page.tsx:226` | 코인별 상한/예산 설계가 사실상 단일 코인 문제로 축소됨 |
| **N-7** | DEEP CORE Phase 0은 **이미 구현·커밋된 자산**이다 | `web/src/components/staking/deep-core/` 17개 파일 — 캔버스, HUD, 컨트롤바, 3개 시트(Depot/Crew/Ledger), WellBadge, 씬, 프리퍼런스, 인트로 오버레이 | §11의 화면 요구사항은 이 자산을 **재사용**하는 전제로 작성한다 |
| **N-8** | 스테이킹 관련 마이그레이션이 8건 존재하며 최신은 `20260809045206_staking_auto_renew_status_values` | `web/prisma/migrations/` | 스키마는 라이브 DB에 적용된 상태다. **라이브 포지션 데이터가 존재하는지 여부는 코드로 알 수 없다** → §13 H-5 |
| **N-9 (개정 01)** | **BANA에는 로컬 잔고 원장이 존재하지 않는다.** `schema.prisma` 전체에 `balance` 필드가 **0건**이며, 사용자에게 보이는 잔고는 전량 허브 `GET /api/v1/wallets`에서 온다 | `web/prisma/schema.prisma` (grep 0건), `web/src/app/api/nia/balance/route.ts:14` | **현행 시스템의 잔고 권위(balance authority)는 Nia-Hub 단독이다.** "BANA DB에서 증가시킨다"는 모델은 오늘 존재하지 않는 구조다 → §2.1, B-0 |
| **N-10 (개정 01)** | `forwardWithdrawalToHub`는 **차변(debit) 호출**이다. `userId: wr.niaUserId`를 실어 허브에 출금을 지시하며, 이는 **엔드유저의 허브 잔고를 감소**시키는 동작이다 | `web/src/lib/withdrawals.ts:35-46` → `niaWalletRequest('POST','/api/v1/withdrawals')` | 출금 API는 **크레딧 수단이 아니다.** Q1 답변이 B-1을 해소하지 못하는 이유의 핵심 → §2.1 |
| **N-11 (개정 01)** | 사용자 주도 스테이킹은 **허브 잔고를 요구한다.** 허브 잔고를 조회해 `available = niaBal − locked`를 계산하고, 부족하면 `STAKE_INSUFFICIENT_AVAILABLE`로 거부한다 | `web/src/app/api/staking/stake/route.ts:100-101, 138-144` | **허브가 BANA를 자산으로 인식하지 못하면 사용자 스테이킹이 단 한 건도 성공할 수 없다.** Q3 답변("추가하면 된다")과 코드 사이에 **검증 가능한 모순**이 있다 → B-2-i는 실증 가능 |
| **N-12 (개정 01)** | 출금 수수료·최소 출금 단위는 이미 존재하나 **허브가 공급**한다(`withdrawFee` / `minWithdrawAmount`, `GET /api/v1/markets` 경유). 관리자 추가 커스텀 코인(`ManagedCoin`)은 `fee:'0', min:'0'` 하드코딩이며 스키마에 해당 필드가 없다 | `web/src/components/Withdraw.tsx:97-98, 109`; `schema.prisma` `ManagedCoin.networks` | Q5(관리자 설정 요구)는 **실재하는 공백**이다. 단 허브 값을 대체할 수 없다는 제약이 붙는다 → §8.7 |
| **N-13 (개정 01)** | **리포지토리 전체에 온체인 서명/전송 코드가 존재하지 않는다.** `ethers` / `viem` / `web3` / `privateKey` / `signTransaction` / `JsonRpcProvider` 전수 grep 결과 실질 일치 0건 | 리포지토리 전수 grep | 마스터가 말한 "회사 지갑에서 유저에게 전송"은 **이 코드베이스가 실행하는 동작이 아니다.** 그 지갑이 온체인 지갑이라면 이 시스템은 그것에 프로그래밍적으로 도달할 수 없다 → B-2-ii |

> **N-8의 함의를 정직하게 적는다.** 이 리포지토리에서 "라이브 포지션이 몇 건인가"를 답할 수
> 없다. 따라서 §5의 전환 전략은 **라이브 데이터가 존재한다고 가정**하고 설계되었다. 존재하지
> 않는 것으로 확인되면 전환은 더 쉬워질 뿐, 설계가 틀려지지는 않는다. 반대 방향의 가정
> (데이터가 없다고 가정)은 틀렸을 때 자금 사고가 되므로 채택하지 않는다.

### 2.1 마스터 답변의 판정 (개정 01 — 2026-08-10)

마스터가 `staking-payout-rail-prd.md` §5.1 질의서의 Q1/Q3/Q4/Q5/Q6/Q7에 직접 답변했다.
**답변을 그대로 게이트 해제로 취급하지 않는다** — 각 답변이 실제로 무엇을 답했는지 판정한다.
이 절이 §13 재구성의 근거다.

#### 2.1.1 Q1 — "회사 지갑에서 유저 출금 주소로 전송" (B-1)

> 답변 원문: *"bana wallet 에서 db 상으로 증가만 한다. 유저가 출금 신청을 하면 회사 지갑에서
> 유저의 출금 지갑 주소로 토큰이 전송되는 구조이다."*

**판정: B-1을 해소하지 않는다. 다만 질문의 형태가 바뀐다.**

이유는 하나이고 명확하다. **질문은 "크레딧(credit) 수단이 있는가"였고, 답변은 "인출(debit)
경로"를 서술했다.** 코드상 두 동작은 반대 방향이다:

| | 크레딧 (B-1이 묻는 것) | 인출 (마스터가 서술한 것) |
|---|---|---|
| 동작 | 엔드유저의 **인출 가능 잔고를 증가**시킨다 | 엔드유저의 잔고를 **감소**시키고 외부 주소로 보낸다 |
| 코드 | **존재하지 않는다** | `forwardWithdrawalToHub` → `POST /api/v1/withdrawals` (N-10) |
| 전제 | — | **잔고가 이미 있어야 한다** |

마지막 행이 결정적이다. 출금 경로는 **이미 잔고가 있는 자금**을 밖으로 내보내는 수단이다.
스테이킹 이자는 BANA Postgres에만 있고 허브 잔고에는 없으므로, 이 경로에 그대로 태울 수 없다.
"출금 API가 있으니 지급할 수 있다"는 추론은 **한 칸 앞의 문제(어떻게 잔고가 되는가)를 건너뛴다.**

그리고 답변의 첫 문장 — *"bana wallet 에서 db 상으로 증가만 한다"* — 은 **현행 코드와 일치하지
않는다.** N-9에 따라 BANA DB에는 증가시킬 잔고 필드가 없다. 증가하는 것은
`StakePosition.paidInterest` / `StakingPayout` 같은 **스테이킹 원장**뿐이며, 그 값은 사용자
잔고 화면에도 출금 가용액 산식에도 들어가지 않는다. **즉 마스터의 서술은 "현재 이렇게
동작한다"의 기술이 아니라 "이렇게 되어야 한다"는 의도의 기술로 읽어야 한다.**

따라서 이 답변은 B-1을 닫는 대신 **그보다 상위의 질문을 연다**:

> **B-0 (신규, 실질 최우선). 사용자 출금 가능 잔고의 권위(authority)는 누구인가?**
>
> - **(A) 허브 권위** — 현행 코드. 허브 잔고가 진실이고, BANA DB는 원장·정책만 보유한다.
>   이 경우 이자를 지급하려면 **반드시 허브 잔고를 증가시키는 수단**이 필요하다 → B-1′ 존속.
> - **(B) BANA DB 권위** — 마스터 답변을 문자 그대로 구현한 모델. BANA DB의 숫자가 진실이고,
>   출금 시 그 숫자를 근거로 회사 자금에서 송금한다. 이 경우 허브 크레딧 API는 필요 없다.

**(B)를 선택하는 것은 커스터디 모델 변경이며, v2의 명시적 비목표(§15)다.** 가볍게 넘길 사안이
아닌 이유를 적는다:

1. **잔고 원장이 두 개가 된다.** 허브도 잔고를 갖고 있고(N-9 — 입금·거래·출금이 모두 허브를
   지난다) BANA DB도 갖게 되면, 두 숫자가 어긋날 수 있다. **그 어긋남이 정확히 이 PRD가
   제거하려는 결함 유형이다.** 결함을 고치려다 같은 결함을 한 층 위에 복제하게 된다.
2. **출금 가용액 산식이 무효가 된다.** `available = niaBal − locked`(N-4)는 허브 권위를 전제한
   식이다. (B)에서는 이 식이 틀리며, **v2가 "출금 라우트를 한 줄도 건드리지 않는다"고 한 D-2의
   핵심 이점(§4.1)이 소멸한다.**
3. **허브가 그것을 수용하는지 미확인이다.** (B)는 "엔드유저의 허브 잔고보다 많은 금액의 출금을
   허브가 실행해 주는가"를 요구한다. 이는 **허브에 대한 새로운 질문**이며 현재 답이 없다.
4. **`web/src/lib/staking.ts` / `stake/route.ts`도 함께 무효가 된다.** 스테이킹 체결 자체가 허브
   잔고를 읽어 판정하므로(N-11), 잔고 권위가 바뀌면 체결 경로도 재작성 대상이다.

**PM 권고: (A) 허브 권위를 유지한다.** (B)는 v2 범위에서 처리 불가하며, 채택하려면 별도 PRD +
`wallet-security-expert` 리뷰 + 재무 승인이 필요하다. 그러나 **이것은 PM이 단독 결정할 사안이
아니다** — 마스터의 답변이 (B)를 가리키고 있으므로, 선택은 마스터에게 되돌린다(§13 B-0).

#### 2.1.2 Q3 — "BANA 토큰을 자산으로 취급할 수 있다. 추가하면 된다" (B-2 전반부)

**판정: 부분 진전. 그러나 "추가하면 된다"는 "아직 추가되지 않았다"는 뜻이므로, 이는 사실
확인이 아니라 미완 작업의 통지다.** 게이트를 닫지 않는다.

그리고 여기에 **코드와의 모순**이 있다(N-11). 사용자 주도 스테이킹은 허브의 BANA 잔고를 읽어
`available`을 계산하고 부족하면 거부한다. 허브에 BANA가 자산으로 등록되어 있지 않다면
`GET /api/v1/wallets?currency=BANA`가 잔고를 반환하지 않고, **모든 사용자 스테이킹이
`STAKE_INSUFFICIENT_AVAILABLE`로 실패**한다. 따라서 다음 둘 중 하나다:

- (i) BANA는 **이미 등록되어 있고**, 마스터의 답변은 다른 층위(예: 스테이킹 대상 코인 목록에
  추가하는 것)를 가리킨다 → B-2-i는 사실상 해소.
- (ii) BANA는 등록되어 있지 **않고**, 그렇다면 **오늘까지 사용자 주도 스테이킹이 성공한 적이
  없다**(관리자 그랜트 포지션만 존재할 수 있다 — 그랜트 경로는 허브 잔고를 확인하지 않는다).

**이 둘은 프로덕션에서 즉시 판별 가능하다.** 추측하지 않고 실증 절차를 §14에 넣는다.
(ii)로 판명되면 부채 사이징(B-5)과 그랜트 락 문제(B-4)의 그림이 통째로 바뀐다.

#### 2.1.3 Q4 — "모든 토큰은 Bana Wallet의 admin 지갑 주소에 모두 존재한다" (B-2 후반부 / G-0′)

**판정: 해소하지 않는다. 오히려 미확인 영역을 넓힌다.**

답변은 **"admin 지갑 주소"** 라는 표현을 쓴다. 주소(address)는 **온체인 개념**이며, 허브
계정(account)과 같지 않다. 두 해석이 가능하고 **함의가 정반대다**:

| 해석 | 내용 | 함의 |
|------|------|------|
| **(가)** Nia-Hub가 BANA 명의로 운영하는 **허브 측 계정** | 허브 API로 도달 가능할 수 있다 | B-1′("그 계정에서 엔드유저로 크레딧하는 API가 있는가")만 답하면 된다 |
| **(나)** BANA가 별도로 보유한 **온체인 지갑** | **이 코드베이스는 도달할 수 없다** — 서명/전송 코드가 0건(N-13) | 지급은 이 시스템 밖의 수동·별도 인프라 작업이 되고, v2의 자동 클레임 레일(§4)이 성립하지 않는다 |

**어느 쪽인지 이 리포지토리로는 판정할 수 없다.** 다만 코드가 주는 정황은 (가) 쪽이다 —
`CLAUDE.md`가 Nia-Hub를 "B2B crypto wallet platform"으로 기술하고, `NIA_BROKER_ID` 환경변수와
브로커 대시보드 개념이 존재하며, 입금·잔고·출금·거래가 전부 허브를 지나고, BANA 측에는
커스터디 코드가 한 줄도 없다. **그러나 정황은 확정이 아니다. 미확인으로 남긴다.**

남는 기술적 질문은 사용자가 정확히 지적한 그대로다:

> **그 지갑/계정에서, 실제 외부 온체인 입금 없이, 특정 엔드유저의 인출 가능 잔고만 증가시키는
> 프로그래밍적 수단이 있는가?**

이것이 §13.1 질의서 1번의 문장이 된다.

#### 2.1.4 Q5 — "이체 수수료·최소 이체 단위는 admin에서 설정할 수 있어야 한다"

**판정: 사실 확인이 아니라 신규 제품 요구사항이다.** 파트너 질의 항목에서 빼고 §8.7(데이터
모델) + §11.6(관리자 화면)으로 편입한다. 근거: N-12 — 현재 이 값들은 허브가 공급하며,
관리자 추가 코인에는 값 자체가 없다(하드코딩 0).

단, **요구사항을 그대로 수용하지 않고 두 개의 제약을 붙인다.** 둘 다 자금 사고 방지 목적이다.

1. **허브 값을 대체할 수 없다.** 허브의 `withdrawFee`는 온체인 실비다. 관리자가 그보다 낮은
   값을 설정하면 차액을 플랫폼이 조용히 부담하거나 전송이 실패한다. → 가산·상향만 허용(T-2).
2. **클레임 수수료는 계약 조건이다.** 계약된 이자를 수령하는 데 수수료를 물리면 그것은
   **실질 이율 인하**이며, A1′(체결 후 밴드 불변)의 위반이다. → 기본값 0, 도입 시 체결 시점
   공시 + 포지션 스냅샷, 기존 포지션 소급 적용 금지(T-6).

#### 2.1.5 Q6 / Q7 — 마스터가 PM 판단에 위임

두 항목 모두 **"파트너 답변을 기다리지 않아도 되는 설계"** 로 종결한다. 상세는 §4.3 C-10/C-11.

- **Q6(실패 조회 status 엔드포인트)** → **존재 여부에 의존하지 않는다.** C-4가 이미 fail-closed
  (모호한 실패는 재시도 없이 FAILED + 수동 검토)이므로, status 엔드포인트는 **운영 편의**일 뿐
  설계 입력이 아니다. 있으면 쓰고, 없으면 수동 대사한다. → **차단 해제.**
- **Q7(부분 성공)** → **상태기계에 `PARTIAL`을 만들지 않는다.** 허브가 보고한 크레딧 금액을
  신뢰해 부분 금액을 원장에 기록하는 것을 금지한다(우리가 검증할 수 없는 값이다). 결과는
  SUCCEEDED(요청 전액) 또는 FAILED(수동 검토) 둘뿐이고, 부분 성공은 **대사에서 발견되는
  불일치**로 처리한다. → **차단 해제.**

> **이 두 판정의 공통 원리를 적어 둔다.** *"파트너가 X를 제공하는지 모른다"가 설계를 막는다면,
> **X가 없어도 안전한 설계**가 존재하는지 먼저 확인한다.* 여기서는 존재했다 — 두 경우 모두
> "모르면 멈추고 사람이 본다"가 정답이고, 그 정답은 X의 존재 여부와 무관하다. 답을 기다릴
> 필요가 없는 질문을 차단 목록에 남겨 두면 게이트가 실제보다 무거워 보인다.

#### 2.1.6 종합 — 게이트에 미친 순효과

| 항목 | 개정 전 | 개정 후 |
|------|---------|---------|
| B-1 (크레딧 API) | 미확인 · 차단 | **미확인 · 차단 유지**, 단 상위에 **B-0(잔고 권위)** 가 선행 항목으로 신설 |
| B-2 (BANA 자산 취급) | 미확인 · 차단 | **B-2-i로 분리** — "추가하면 된다" = 미확정. **프로덕션에서 실증 가능**(N-11) |
| B-2 (트레저리 계정) | 미확인 · 차단 | **B-2-ii로 분리** — "admin 지갑"의 성격(허브 계정 vs 온체인 지갑)이 **미확인**. 차단 유지 |
| B-2b (수수료·최소 단위) | 파트너 질의 | **제품 요구사항으로 전환**(§8.7). 잔여 파트너 질의는 "크레딧 수단에 수수료가 붙는가" 한 줄뿐 |
| B-2b (status 엔드포인트) | 파트너 질의 · 차단 | **PM 판정으로 종결**(C-10). 차단 해제 |
| B-2b (부분 성공) | 파트너 질의 · 차단 | **PM 판정으로 종결**(C-11). 차단 해제 |

**차단 항목 7개 → 4개(B-0 / B-1′ / B-2-i / B-2-ii). 그중 B-0은 마스터의 결정, B-2-i는 실증
가능, 파트너 회신이 반드시 필요한 것은 B-1′와 B-2-ii 둘뿐이다.**

---

## 3. 핵심 모델 결정 — 계약 수익 밴드 (Contracted Yield Band)

### 3.1 결정

> **상품은 단일 이율이 아니라 밴드를 제공한다. 체결 시점에 `기준 일이율`(보장 하한)과
> `최대 가산율`(상한)이 함께 스냅샷되어 포지션에 고정된다. 게임 진행도(MP)는 그 밴드
> **안에서의 위치**만 결정하며, 밴드 자체는 체결 이후 어떤 사유로도 변경되지 않는다.**

```
상품 예시 (표기)
  BANA 30일 정기
  기준 일이율 0.100 %   (보장)
  최대 가산   0.010 %p  (기준 이자의 10.00% 상당, DEEP CORE 진행도에 따라 0 ~ 최대)
  → 실 수령 일이율 0.100 % ~ 0.110 %
```

일별 정산:

```
baseAmount(p, d) = dailyInterest(p.principal, p.baseDailyRatePct)      // 기존 stakingMath 그대로
bonusPct(MP)     = p.maxBonusPctOfBase × (clamp(MP, MP_MIN, MP_MAX) − MP_MIN) / (MP_MAX − MP_MIN)
bonusAmount(p,d) = floor( baseAmount × bonusPct / 100 )                 // 코인 정밀도로 내림
dayTotal(p, d)   = baseAmount + bonusAmount
```

- `MP_MIN=5`, `MP_MAX=500`, 선형·하드캡 — **04 문서 §2.1의 수식 형태를 그대로 승계한다.**
  바뀐 것은 상한이 전역 상수(`GAME_BONUS_MAX_PCT`)가 아니라 **포지션의 계약 스냅샷**
  (`maxBonusPctOfBase`)이라는 점 하나다.
- 전역 코드 상수 `GAME_BONUS_MAX_PCT = 10.00`은 폐기하지 않는다. **역할이 바뀐다**:
  런타임 지급 상한 → **상품 생성 시 검증 상한**(`maxBonusPctOfBase ≤ GAME_BONUS_MAX_PCT`).
  즉 상한은 두 겹이 되며, 둘 다 불변 데이터에서 나온다(코드 상수 + 체결 스냅샷).
- 모든 산술은 `decimal.js`(CLAUDE.md 규칙 2). 반올림은 **내림**(04 §2.2 승계).

### 3.2 왜 "얹기"가 아니라 "밴드"인가 — 04 문서의 A1을 폐기하지 않고 강화한다

04 문서 A1("계약 이율 불변")은 옳은 원칙이었다. 그러나 그 원칙을 지키는 방법으로 04는
**"게임을 계약 밖에 둔다"** 를 선택했고, 그 선택의 대가가 다음 세 가지였다.

1. **지급 스트림이 둘이 된다.** `StakingPayout` + `GameBonusPayout`. 우리가 지금 고치고 있는
   결함(원장 ↔ 실잔고 불일치)이 **두 개의 원장에 각각** 존재하게 된다. 대사(reconciliation)
   표면이 2배, 클레임 상태기계도 2배.
2. **보너스가 재량적(discretionary) 지급이 된다.** 04 §5.4는 예산 초과 시 `gameBonusEnabled`를
   자동 false로 전환한다. 즉 사용자 입장에서 이 보너스는 **플랫폼이 언제든 끌 수 있는 것**이다.
   금융 상품에 붙은 재량적 지급은 밴드보다 **더** 다루기 어려운 물건이다 — 언제 끊길지 모르는
   수익을 근거로 자금을 잠그게 만들기 때문이다.
3. **최대 부채가 정의되지 않는다.** 04 §5.4의 월 예산은 "초과하면 멈춘다"는 사후 브레이크일 뿐,
   **체결 시점에 이 포지션이 만들 수 있는 최대 부채가 얼마인지 아무도 계산할 수 없다.**
   내 자신의 패턴("발행을 시작하기 전에 지급 방법이 확정되어 있어야 한다 / 규모가 통제 밖에서
   accretion으로 결정된다")이 정확히 이 지점을 경고한다.

밴드 모델은 셋을 동시에 해소한다.

| 문제 | 밴드 모델에서 |
|------|---------------|
| 지급 스트림 2개 | **1개.** `dayTotal` 단일 금액이 하나의 원장 행에 기록된다. 클레임도 하나 |
| 재량적 지급 | **계약 요소.** 체결 시 상·하한이 공시되고, 하한은 보장된다. "끌 수 있는 것"은 **신규 밴드 상품의 제공**뿐이며, 이미 체결된 포지션의 밴드는 만기까지 유지된다 |
| 최대 부채 미정의 | **체결 시점에 계산 가능.** `maxLiability(p) = (baseAmount × (1 + maxBonusPctOfBase/100)) × termDays`. 이 값을 체결 시 예약(reserve)할 수 있다 → §7.2 |

세 번째가 결정적이다. **밴드 모델은 게임 보너스를 "무한정 열린 프로그램 부채"에서
"포지션 단위로 사전 계산·사전 충당 가능한 확정 상한 부채"로 바꾼다.** 04의 G-3(월 예산 확정)이
"매달 얼마를 쓸 것인가"라는 답하기 어려운 질문이었던 이유는, 그 모델에서는 노출이 사용자 행동에
따라 사후적으로만 결정되기 때문이다. 밴드 모델에서 G-3은 **"예약 풀에 얼마를 넣을 것인가"** 라는
훨씬 답하기 쉬운 질문이 된다.

### 3.3 계약 불변성의 재정의 (04 A1 → v2 A1')

> **A1' (v2). 게임은 `baseDailyRatePct`, `maxBonusPctOfBase`, `termDays`, `principal`,
> `maturityAt` 어느 것도 읽기 외의 방식으로 건드리지 않는다. 체결 이후 밴드는 폭도 위치 기준도
> 변하지 않는다. 게임이 결정하는 것은 밴드 내 좌표뿐이다.**

04의 규칙 Y-1이 "값을 건드리지 않는다"였다면, A1'는 "**구간을 건드리지 않는다**"이다.
불변성의 대상이 점에서 구간으로 바뀌었을 뿐, 강도는 동일하다. 그리고 사용자에게는 **더 나은**
불변성이다 — 04 모델에서 사용자는 밴드의 존재 자체를 계약으로 보장받지 못했다.

> **개정 01 추가.** A1′의 적용 대상에 **클레임 수수료**가 포함된다. 수령 단계에서 부과되는
> 수수료는 실질 수령 이율을 낮추므로, 체결 후 도입·인상은 밴드를 아래로 좁히는 것과 동일하다.
> → §8.7 T-6.

### 3.4 승계되는 04 문서의 안전장치

04 §1~§9의 다음 항목은 **전부 그대로 승계한다.** 재검토 대상이 아니다.

| 04 ID | 내용 | v2에서 |
|-------|------|--------|
| Y-2 | 보너스는 항상 ≥ 0. 게임 미이용이 페널티가 되지 않는다 | **승계 + 강화.** 밴드 하한(기준 이율)이 계약상 보장이 되므로, 게임을 전혀 하지 않는 사용자의 수령액이 **계약으로** 보호된다 |
| Y-3 / Y-4 | 복리 없음. 보너스가 보너스를 낳지 않음 | 승계. N-1로 갱신 경로도 복리가 아님이 확인됨 |
| A3 | 확률형 요소 전면 배제 | 승계 |
| A4 | 수익 영향 레이어는 영구·확정·무기한. FOMO 장치 금지 | **승계 + 강화.** 밴드가 계약 요소가 되므로 긴급성 연출은 더더욱 금지 |
| A5 | 리더보드·랭킹·타 사용자 비교 없음 | 승계 |
| A6 | 끊기는 스트릭 없음. 누적 오도미터만 | 승계 |
| A7 | 원금 비례 성장 금지 (XP·CC는 정수·코인 무관 소스에서만) | 승계 |
| A8 | 최대치가 시간만으로 도달 가능 | 승계 |
| A9 | 소급 적용 금지. `mpEffectiveFrom` 승격 게이트 | **승계 — 필수.** 밴드 모델에서 소급은 계약 위반이 된다 |
| A13 / A14 | 고지는 DOM 오버레이, NPC 대사 금지 | 승계 |
| §4.2 | `mp` / `mpEffective` / `mpEffectiveFrom` 2필드 스냅샷 | 승계 |
| §4.3 | 행 단위 `mpSnapshot`·`bonusPctSnapshot` 기록, 과거 행 재계산 금지 | 승계 |
| AB-1~AB-7, AB-10~AB-13, AB-15 | 남용 방어 | 승계 (§7.4에 재배치 항목만 별도 표기) |

### 3.5 폐기·재정의되는 04 문서 항목

| 04 ID | 원 내용 | v2 판정 |
|-------|---------|---------|
| **Y-1** | 게임은 `dailyRatePct`/`paidInterest`/`StakingPayout`을 건드리지 않는다. 보너스는 분리 원장 `GameBonusPayout`에만 존재 | **폐기 → A1'로 대체.** 분리 원장을 만들지 않는다 |
| **§6 스키마** | `GameBonusPayout` 테이블 | **폐기.** 밴드 금액은 통합 원장 행의 컬럼이 된다(§8) |
| **A12 / §5.4** | 관할별·예산 킬 스위치로 보너스 지급을 **장래효 정지** | **재정의.** 이미 체결된 포지션의 밴드는 정지할 수 없다. 킬 스위치는 **신규 밴드 상품 제공 중단**에만 작용한다(§7.5) |
| **§5.3 상한 3** | 사용자·일·코인 절대 상한으로 지급액을 **절단** | **폐기 → 인수 통제로 이동**(§7.2). 계약된 금액은 자를 수 없다 |
| **§5.4 상한 4** | 프로그램 월 예산 초과 시 자동 정지 | **폐기 → 예약 풀로 이동**(§7.2) |
| **A10 / E-2** | 관리자 그랜트 포지션 보너스 제외 | **승계하되 표현 변경.** 그랜트 포지션은 `maxBonusPctOfBase = "0"`으로 체결된다(별도 분기 불필요) |
| **A11 / Y-5** | 게임 보너스는 레퍼럴 산정 기준액에서 제외 | **승계 — 필수.** §3.6 참조 |
| **§8.1 고지 6종** | 상시 고지 문자열 | **3종 유효, 1종 폐기, 2종 재작성.** §10 참조 |
| **M-3 (00 §6.3)** | 게임 보너스 누계를 계약 이자와 합산 표시 금지 | **재해석.** 밴드 모델에서 둘은 하나의 지급액이다. 그러나 **일별 내역에서 base/bonus 분해는 여전히 필수**다(§11 R-U6) |
| **EQ-1** | 보너스의 실제 정산 경로 미확인 | **이 문서 §4가 답이다** |

### 3.6 레퍼럴 기준액 — 밴드 모델에서 더 중요해진다

`payReferralBonuses`는 이자의 %를 상위 라인에 지급한다. 04 Y-5는 게임 보너스를 기준액에서
제외했다. 밴드 모델에서는 보너스가 **계약 이자의 일부**가 되므로, 순진하게 구현하면
`SUM(amount)`를 읽어 자동으로 포함되어 버린다.

> **규칙 Y-5' (필수).** 레퍼럴 산정 기준액은 `SUM(baseAmount)`이며, `SUM(amount)`가 아니다.
> 가산분은 어떤 경우에도 MLM 발행량의 배수기가 되어서는 안 된다.

이것이 §8에서 `baseAmount`를 **별도 컬럼으로** 저장해야 하는 이유다. `amount − bonusAmount`로
유도 가능하더라도 명시 컬럼을 둔다 — 유도식은 언젠가 한쪽에서 잊힌다.

### 3.7 MP 단조증가 요구 (v2 신규)

XP는 감소하지 않고(02 P-3), 장비는 소멸하지 않는다(03 C-4/C-5). 따라서 MP는 구조적으로
**단조 비감소**다. v2에서는 이것이 우연이 아니라 **요구사항**이 된다.

> **규칙 Y-6 (v2 신규). MP를 감소시킬 수 있는 어떤 메커니즘도 도입하지 않는다.**
> 밴드 모델에서 MP 하락은 곧 **사용자가 이미 관측한 일별 수령액의 하락**이며, 그것은 계약
> 조건이 나빠진 것처럼 경험된다. 소모품·차감·시즌 리셋·감쇠(decay)는 전부 금지한다.

이 규칙은 03 §5.3(소모품 카테고리 자체를 만들지 않음)을 계약 관점에서 재확인한 것이다.

---

## 4. 지급 레일 — Option C(명시적 클레임) 유지

### 4.1 결정

재구축 기회에도 **Option C를 유지한다.** 기존 PRD §3 D-3의 근거 5가지는 v2에서도 전부 유효하며,
두 가지가 추가로 강화된다.

| 근거 | v2에서의 상태 |
|------|---------------|
| 출금 가용액 산식(`available = niaBal − locked`)을 바꾸지 않아도 된다 | **유효 — 단 B-0가 (A) 허브 권위일 때만.** 클레임 완료분은 평범한 허브 잔고이므로 산식이 이미 정확하다. B-0가 (B)로 결정되면 이 근거는 소멸한다(§2.1.1) |
| 외부 호출이 단일·저빈도·사용자 트리거로 격리 | **유효** |
| 먼지 이체 소멸 | **유효 — 그리고 더 중요해졌다.** 자동 지급이었다면 이제 base와 bonus 두 금액을 매일 이체하거나 합산 이체해야 하고, 소액 포지션의 일별 가산분은 base보다 한 자릿수 작다 |
| UI 정직성이 구조적으로 확보 | **유효** |
| 메커니즘 불가지론(B-1 답변 없이 제품 설계 확정 가능) | **유효 — 여전히 결정적이다.** B-1′/B-2-ii는 개정 01 시점에도 미확인이다 |
| **(v2 추가)** 밴드 모델로 스트림이 하나가 되어 클레임 대상이 단일화 | 04 모델이었다면 "계약 이자 클레임"과 "게임 보너스 클레임"의 관계를 정의해야 했다. 밴드 모델에서는 그 질문 자체가 없다 |
| **(v2 추가)** 클레임 시점 = 부채 확정 시점 | 예약 풀(§7.2)의 해제 시점을 클레임 성공에 묶을 수 있어, 예약 ↔ 실지급의 대사가 자명해진다 |

### 4.2 자동 지급을 채택하지 않은 이유 (재구축 기회 검토 결과)

마스터가 명시적으로 물은 항목이므로 재검토했다. **채택하지 않는다.**

- 자동 지급은 **무인 배치가 사용자 자금을 외부로 이동**시키는 구조다. 정산 워커는 현재 외부
  I/O가 0이고 기본 5분 주기로 무인 실행된다. 재구축이라는 사실이 이 위험을 낮추지 않는다 —
  오히려 새 코드에 이 위험을 넣는 것이 더 나쁘다(검증 이력이 없다).
- **B-1/B-2가 미확인인 상태에서 자동 지급은 설계조차 확정할 수 없다.** 수수료·최소 이체
  단위·실패 조회 수단을 모르면 무인 배치의 실패 처리 정책을 쓸 수 없다. 반면 클레임은 이
  답들 없이도 제품 표면(버튼 하나)이 확정된다.
- 자동 지급의 유일한 장점(미청구 잔액이 남지 않음)은 **관리자 리포트에서 미청구 잔액을 상시
  가시화**하는 것으로 충분히 관리 가능하다.

**단, 하나는 v2 범위에 넣는다:** 만기 시점의 **1회성 클레임 유도 알림**(이메일 또는 인앱 배너).
자동 이체가 아니라 "받아가세요" 안내다. 무인 자금 이동이 아니므로 위 리스크가 없다.
자동 클레임(무인 이체)은 **v2 비범위**이며, 미청구 잔액이 커지면 별건으로 재검토한다.

### 4.3 클레임 규약 (기존 PRD R-10 승계 + v2 추가)

| ID | 요구사항 |
|----|----------|
| **C-1** | `POST /api/staking/claim` — 세션에서 userId 도출. 클라이언트 값 신뢰 금지(CLAUDE.md 규칙 8) |
| **C-2** | 원자적 상태 전이(`updateMany where status`, `count === 1`일 때만 진행). `WithdrawalRequest` 패턴 승계 |
| **C-3** | 안정적 멱등키를 허브 호출에 전달 |
| **C-4** | **모호한 실패(타임아웃/네트워크)는 재시도 없이 FAILED 종결 + 수동 검토.** `forwardWithdrawalToHub`(`web/src/lib/withdrawals.ts`)와 동일 규약 |
| **C-5** | 클레임 단위는 **사용자 × 코인 단위 미청구 총액 일괄**이다. 포지션별 클레임을 만들지 않는다(먼지 이체 방지). **개정 01: C-11의 1회 클레임 상한이 설정된 경우, 그 상한 범위 안에서의 일괄로 읽는다** |
| **C-6** | 클레임 활성화 킬 스위치(`PlatformSetting`). 기본 **비활성**. `maintenanceMode` 시 차단 |
| **C-7** | **클레임을 게임화하지 않는다.** 스트릭·클레임 보너스·지연 보상·연출 보상 금지. DEEP CORE는 클레임 상태를 읽지도 쓰지도 않는다 (기존 D-4 승계) |
| **C-8** | 모든 금액 연산 `decimal.js`. `Number()`/`parseFloat()` 금지 |
| **C-9** | 불변식: 사용자별 `claimedYield ≤ ledgeredYield`. 플랫폼 전체 `SUM(SUCCEEDED 클레임 금액) == SUM(claimedYield)` — 자동 검증 가능해야 한다 |
| **C-10 (개정 01 — Q6 판정)** | **허브의 실패 조회(status) 엔드포인트 존재 여부에 설계가 의존하지 않는다.** C-4가 이미 fail-closed이므로 status 엔드포인트 없이도 안전하다. 존재하면 운영 편의로 활용하되 **설계 입력으로 삼지 않는다.** 대신 수동 대사가 가능하도록 클레임 행은 ① 전송 페이로드 요약 ② 전송 시각 ③ 멱등키 ④ 허브 응답 원문(또는 오류 메시지)을 보존해야 한다. **수동 대사 런북은 배포 전 필수 산출물**이며, 그것 없이 킬 스위치를 켜지 않는다 |
| **C-11 (개정 01 — Q7 판정)** | **상태기계에 `PARTIAL` 상태를 만들지 않는다.** 허브가 보고한 크레딧 금액을 신뢰해 부분 금액을 원장에 기록하는 것을 **금지한다** — 우리가 독립적으로 검증할 수 없는 값이기 때문이다. 결과는 **SUCCEEDED(요청 전액)** 또는 **FAILED(수동 검토)** 둘뿐이며, 부분 성공은 대사에서 발견되는 **불일치**로 처리한다. 수동 대사 규모를 유한하게 만들기 위해 **1회 클레임 상한**(§8.7 T-1/T-7)을 둔다. 상한 초과 시 조용히 절단하지 않고 **분할 클레임을 명시적으로 안내**한다 |

### 4.4 B-1이 "그런 API는 없다"로 확정될 경우

기존 PRD와 동일하다. 그 경우 자체 커스터디 지갑·온체인 이체·가스비 시나리오로 넘어가며,
이는 제품·재무·Nia-Hub 3자 협의 사안이다. **v2의 §3(밴드 모델)·§5(전환)·§6(그랜트)·§11(화면)은
그 답과 무관하게 유효하지만, §4(레일)와 밴드 상품 출시는 전면 재작성 대상이 된다.**
그리고 그 경우 Track 1(허위 표시 정정)의 중요도는 **더 올라간다** — 지급 시점이 무기한 미정이
되기 때문이다.

> **개정 01 갱신.** `staking-payout-rail-prd.md` §5.1의 질의 6개에 대해 마스터가 답변했으나,
> 그 답변은 **파트너의 확인이 아니라 마스터 자신의 이해·의도를 서술한 것**이다(§2.1). 따라서
> 파트너 질의는 여전히 필요하며, **§5.1을 대체하는 갱신 질의서는 §13.1(4문항)이다.**
> 그리고 N-13(온체인 서명 코드 0건)에 의해, "자체 커스터디 지갑·온체인 이체" 시나리오는
> **이 리포지토리에 코드가 전혀 없는 상태에서의 신규 인프라 구축**을 뜻한다 — 기존 PRD가
> 쓰였을 때보다 더 무거운 시나리오임을 명시해 둔다.

---

## 5. 전환 전략 — 통합 경로(밴드폭 0)

### 5.1 결정

**기존 포지션을 별도 시스템으로 남기지 않는다. 별도 정산 엔진을 운영하지 않는다.**

> **기존 포지션 = `maxBonusPctOfBase = "0"` 인 v2 포지션.**
> 밴드폭이 0이면 `bonusPct`가 항상 0이고 `dayTotal == baseAmount`가 되어, 정산 결과가 현행과
> **비트 단위로 동일**하다. 즉 기존 사용자의 계약은 문자 그대로 한 글자도 바뀌지 않는다.

세 가지 대안을 기각한 이유:

| 대안 | 기각 사유 |
|------|-----------|
| **빅뱅 마이그레이션** (전 포지션을 밴드 상품으로 전환) | 사용자 동의 없이 체결된 계약의 조건을 바꾸는 것이다. 유리한 방향의 변경이라도 계약 변경은 계약 변경이다 |
| **완전 병행 운영** (구 시스템 유지 + 신규만 v2) | 정산 엔진 2벌, 원장 2벌, 클레임 2벌. 우리가 고치려는 결함이 정확히 "원장이 여러 개인데 하나만 실체가 있다"는 것이다 |
| **기존 포지션 조기 정산 후 재개설** | 원금 이동과 사용자 재동의가 필요하고, 그동안 이자 발생이 끊긴다. 사용자에게 손해다 |

### 5.2 원장 승계

**`StakingPayout`을 v2의 원장으로 그대로 승계한다. 새 원장 테이블을 만들지 않는다.**

근거:
1. 이미 올바른 멱등 형태를 갖고 있다 — `@@unique([positionId, dayIndex])`.
2. **기존 부채 전체가 이미 이 테이블에 있다.** 새 테이블을 만들면 자금 행 백필이 필요해지고,
   그것이 정확히 §1.1이 피하려는 위험한 일괄 작업이다.
3. 테이블이 하나면 대사 표면도 하나다.

필요한 것은 컬럼 추가 + **메타데이터 백필**뿐이다(금액 불변):

| 컬럼 | 기존 행 백필값 | 의미 |
|------|----------------|------|
| `baseAmount` | `= amount` | 기준 이자분. 레퍼럴 기준액(Y-5')·감사의 기준 |
| `bonusAmount` | `"0"` | 밴드 가산분 |
| `mpSnapshot` | `null` | 해당 일의 유효 MP (밴드폭 0이면 무의미) |
| `bonusPctSnapshot` | `"0"` | 해당 일 적용 가산율 |

`amount`의 의미는 **유지**한다(= 그날 원장에 기록된 총액 = `baseAmount + bonusAmount`).
기존 행은 `bonusAmount = 0`이므로 항등식이 자동 성립한다.

### 5.3 `paidInterest` 재계산 산식의 강제 소멸 (F-C 해결)

현행 `paidInterest = perDay × dueDays`(N-2)는 **일별 금액이 상수라는 가정** 위에 서 있다.
밴드 모델에서는 MP가 오르면 일별 금액이 달라지므로 이 산식이 **수학적으로 성립하지 않는다.**

> **요구: 포지션의 원장 누계는 재계산이 아니라 `SUM(StakingPayout.amount)`의 누적으로만
> 유지한다.** 기존 PRD F-C가 "구현자의 자연스러운 접근(증분 훅)이 틀린다"고 경고했던 항목이,
> v2에서는 **틀린 접근이 아예 컴파일되지 않는 구조**가 된다.

이것은 밴드 모델의 부수효과 중 가장 값진 것이다 — 설계 변경이 기존 결함 하나를 **선택이 아니라
구조로** 제거한다.

### 5.4 필드 명명 — 지급을 함의하는 이름 금지

현행 `paidInterest`/`paidAt`/`daysPaid`/`totalPaid`는 전부 **지급되지 않은 것을 지급이라 부른다.**
v2는 새 시스템이므로 이 부채를 승계하지 않는다.

> **요구:** 원장 누계 필드는 지급을 함의하지 않는 이름을 쓴다(예: `ledgeredYield`).
> 실제 허브로 이체 완료된 누계는 별도 필드(예: `claimedYield`)로 두며, **`ledgeredYield`의
> 델타로 유도하지 않고 성공한 클레임 금액으로만 증가**시킨다.
> 관리자 통계의 `"totalPaid"` 별칭도 함께 정정한다.

**정확한 컬럼명·마이그레이션 방식(rename vs. add+deprecate)은 `prisma-db-expert`가 결정한다.**
라이브 DB의 컬럼 rename은 위험도가 있으므로 PM이 이름을 못 박지 않는다. 못 박는 것은
**"지급을 함의하는 이름을 쓰지 않는다"는 제약**이다.

### 5.5 `accruedInterest`의 처리

`accruedInterest`(워커가 갱신 + 클라이언트가 읽기 시 재계산)는 v2에서 **문제가 된다.**
밴드 모델에서는 미래의 MP를 알 수 없으므로 **클라이언트가 미래 수익을 외삽할 수 없다.**

> **요구 R-U7 (§11에도 재기재).** UI는 **미확정 수익을 추정해 표시하지 않는다.** 원장에 기록된
> 금액과 **오늘 적용 중인 이율**만 표시한다. 초 단위로 증가하는 "지금 벌고 있는" 카운터는
> 밴드 모델에서 거짓이 되므로 만들지 않는다.

`accruedInterest` 컬럼 자체의 존치 여부는 `prisma-db-expert` 판단에 맡기되, **읽기 시 재계산으로
"실시간 수익" 을 표시하는 코드 경로는 제거한다.**

---

## 6. 그랜트 락(B-4) — 구조적 재설계

### 6.1 문제 재확인

`POST /api/admin/staking/positions`는 허브 잔고 확인 없이 포지션을 생성한다(그랜트 원금은
플랫폼의 의도적 부채). 그런데 `lockedPrincipalByCoin`(N-3)은 그랜트 여부를 구분하지 않고
모든 ACTIVE 포지션 원금을 합산하며, 출금 라우트는 그 합계를 사용자의 **실제** 허브 잔고에서
뺀다(N-4). 그랜트 원금이 실제로 허브에 입금되지 **않는다면**, 보너스를 받은 사용자는 그 순간부터
자기 돈을 그 금액만큼 인출할 수 없게 된다 — **보상이 제재로 작동한다.**

> **개정 01 관측.** 그랜트 경로가 허브 잔고를 확인하지 않는다는 사실은 §2.1.2의 (ii) 시나리오와
> 맞물린다: 허브에 BANA가 등록되어 있지 않더라도 **그랜트 포지션은 생성될 수 있다.** 즉
> "라이브 포지션이 존재한다"는 사실만으로 "허브가 BANA를 자산으로 취급한다"를 추론할 수 없다.
> B-2-i의 실증은 포지션 존재 여부가 아니라 **`GET /api/v1/wallets?currency=BANA` 응답과
> 사용자 주도 스테이킹 성공 이력**으로 해야 한다.

### 6.2 v2의 구조적 방지책

운영 규칙("그랜트 시 반드시 입금할 것")으로 막지 않는다. **타입으로 막는다.**

> **요구 G-A. 포지션에 자금 출처를 명시하는 필드를 도입한다** (예:
> `fundingSource: USER_HUB | PLATFORM_GRANT`). `grantedByAdminId != null`에서 유도하지 않고
> **명시 필드**로 둔다 — 유도식은 새 그랜트 경로가 생기면 조용히 틀린다.
>
> **요구 G-B. 출금 락 산식은 `fundingSource = USER_HUB` 포지션의 원금만 합산한다.**
> 사용자가 맡기지 않은 돈을 사용자의 잔고에서 잠글 근거가 없다.
>
> **요구 G-C. 불변식 테스트: 어떤 `PLATFORM_GRANT` 포지션도 사용자의 출금 가능액을 감소시키지
> 않는다.** 관리자 그랜트 전후로 `available`이 동일해야 한다. 이 테스트가 없으면 G-B는
> 언젠가 리팩터링에서 사라진다.
>
> **요구 G-D. 밴드 상품에서 `PLATFORM_GRANT` 포지션은 `maxBonusPctOfBase = "0"`으로 체결된다.**
> (04 A10/E-2 승계 — 별도 런타임 분기 없이 데이터로 표현된다.)

G-B는 **락을 완화하는 변경**이므로 `wallet-security-expert` 리뷰가 **필수**다.

### 6.3 남는 결정 — 그랜트 원금은 만기에 어떻게 지급되는가

G-B를 적용하면 락 문제는 사라지지만, **"그랜트 원금 자체를 사용자가 어떻게 받는가"** 는 남는다.
허브에 없던 돈이므로 만기에 "해제"할 대상도 없다. 두 갈래이며 **사람의 결정 사항**이다(§13 H-2).

| 안 | 내용 | 함의 |
|----|------|------|
| **H-2-a** | 그랜트 원금도 만기 시 **클레임 대상 금액**이 된다 | 사용자에게 가장 정직하다. 트레저리 충당 규모에 그랜트 원금 총액이 포함되어야 한다(B-5 사이징에 반드시 포함) |
| **H-2-b** | 그랜트는 **이자만 발생시키는 명목 포지션**이며 원금은 지급 대상이 아니다 | 부채가 작다. 단 **현재 사용자에게 어떻게 고지되고 있는지**를 먼저 확인해야 한다 — "원금을 준다"고 표시했다면 b는 채택할 수 없다 |

**PM 권고: H-2-a.** 단, 채택 여부는 (i) 기존 그랜트가 사용자에게 어떻게 고지되었는지,
(ii) 그랜트 원금 총액이 얼마인지에 달려 있고 둘 다 이 리포지토리에서 답할 수 없다.

---

## 7. 상한 체계의 재구성 — 지급 절단에서 인수 통제로

### 7.1 원리

> **계약된 금액은 자를 수 없다. 자를 수 있는 것은 "얼마나 계약할 것인가"뿐이다.**

04 문서의 4중 상한 중 상한 3(사용자·일 절대 상한)과 상한 4(월 예산 자동 정지)는 **지급 시점에
금액을 절단**하는 장치였다. 이것이 04에서 정당했던 이유는 보너스가 재량적이었기 때문이다.
밴드 모델에서는 같은 장치가 **계약 불이행**이 된다.

따라서 두 상한을 **체결 시점으로 이동**시킨다.

### 7.2 재배치된 상한 체계

| # | 상한 | 작용 지점 | 내용 |
|---|------|-----------|------|
| **L-1** | 비율 상한 (04 §5.1 승계) | **상품 생성** | `maxBonusPctOfBase ≤ GAME_BONUS_MAX_PCT`(코드 상수 10.00). 환경변수·관리자 설정으로 올릴 수 없다. `PlatformSetting`으로는 **낮출 수만** 있다(00 M-2 승계) |
| **L-2** | 포지션 상한 (04 §5.2 승계) | **크레딧 직전 재검증** | `bonusAmount ≤ baseAmount × maxBonusPctOfBase / 100`. 하드 어서션. 계산 경로 버그에 대한 2차 방어 |
| **L-3** | **사용자 밴드 노출 상한** (04 상한 3 대체) | **체결(스테이크) 시점** | 한 사용자가 동시에 보유할 수 있는 **밴드 포지션의 총 원금**(또는 총 최대 가산 부채)에 상한을 둔다. 초과하면 신규 밴드 체결을 거부한다. 이미 체결된 것은 건드리지 않는다 |
| **L-4** | **예약 풀 용량** (04 상한 4 대체) | **체결(스테이크) 시점** | 신규 밴드 포지션 체결 시 `maxBonus(p) = baseAmount × maxBonusPctOfBase/100 × termDays` 를 예약 풀에서 차감(예약)한다. 잔여 용량이 부족하면 **체결을 거부**한다. 만기·클레임 시 실사용분과 미사용분을 정산해 반환한다 |

**L-4가 이 설계의 재무적 심장이다.** 이것이 있으면:
- 플랫폼은 언제나 "지금 체결된 전부에 대해 최대로 지급해야 할 금액"을 **정확히** 알 수 있다.
- G-3("월 예산이 얼마인가")이 **"예약 풀에 얼마를 넣을 것인가"** 로 바뀐다. 후자는 답할 수 있는 질문이다.
- 내 자신의 패턴 경고("규모가 accretion으로 결정된다")가 구조적으로 무력화된다. 예약 없이는
  체결이 안 되므로, 부채는 절대 예약 풀을 넘을 수 없다.

거부 시 사용자 경험: **밴드 상품이 "일시 마감"으로 표시되고, 기준 이율만 제공하는 비밀 상품이
아니라 명시적으로 그렇게 보인다.** 몰래 밴드폭을 0으로 낮춰 체결시키는 것은 **금지**한다
(공시된 것과 다른 조건으로 체결하는 것이 된다).

### 7.3 M-1(먼지 포지션 farming)의 v2 처리

00 §6.3 M-1은 Phase 0에 이미 적용 중이며 v2에서도 유효하다. 추가로:

> **요구 L-5. `maxBonusPctOfBase > 0`인 상품은 `minAmount`가 null일 수 없다.** 상품 생성·수정
> 경로에서 검증한다(N-5 — 현재 nullable이다). 밴드 상품에 최소 예치금이 없으면 먼지 포지션으로
> `charter_open` 계열 적립을 farming할 수 있고, 밴드 모델에서는 그것이 곧 실제 지급액이 된다.

### 7.4 남용 시나리오 — 04 §7 대비 변경분만

04의 AB-1~AB-15는 대부분 그대로 유효하다. 변경·신규만 기재한다.

| # | 시나리오 | v2 방어 |
|---|----------|---------|
| AB-8 | 대규모 예치로 절대 금액 폭주 | **L-3(사용자 밴드 노출 상한) + L-4(예약 풀).** 지급 절단이 아니라 체결 거부로 막는다 |
| AB-9 | 상수 변경으로 상한 무력화 | **L-1 승계 + 강화.** 상수를 올려도 **이미 체결된 포지션의 밴드는 변하지 않는다** — 소급 확대 경로가 구조적으로 없다 |
| AB-14 | 예산 초과 후 사후 회수 요구 | **소멸.** 예약 없이는 체결되지 않으므로 "예산 초과"라는 상태 자체가 발생하지 않는다 |
| **AB-16 (신규)** | 밴드 상품을 체결한 뒤 관리자가 상품의 밴드폭을 낮춰 부채를 줄이려 시도 | **체결 스냅샷이 방어.** 상품 수정은 신규 체결에만 영향. 기존 포지션 재계산 경로가 존재해서는 안 된다 (테스트로 잠근다) |
| **AB-17 (신규)** | 예약 풀 고갈 직전 대량 체결로 풀 선점 | L-3(사용자별 노출 상한)이 1차 방어. 추가로 체결 트랜잭션 내에서 예약 차감을 원자적으로 수행(동시성) |
| **AB-18 (신규)** | 밴드 상품을 체결 → 즉시 해지 → 재체결 반복으로 예약 회전 | **조기 해지 엔드포인트가 존재하지 않는다**(현행). v2에서도 만들지 않는다(§15 비목표) |
| **AB-19 (개정 01 신규)** | 관리자가 클레임 수수료를 사후에 올려 실질 지급액을 줄임 | **§8.7 T-6이 방어.** 클레임 수수료는 체결 스냅샷 대상이며 소급 적용이 금지된다. AB-16과 같은 계열(사후 조건 변경)이며, 수수료 축에서 같은 구멍이 열리지 않도록 명시적으로 막는다 |

### 7.5 킬 스위치의 재정의 (04 A12 / M-4 승계 및 수정)

| 스위치 | v2에서의 작용 |
|--------|---------------|
| `gameEnabled` | 게임 표면 on/off. **밴드 지급에 영향 없음** — 이미 체결된 계약이므로 |
| **밴드 상품 제공** (신규) | **신규 밴드 포지션 체결 차단.** 기존 포지션은 만기까지 계약대로 지급된다 |
| 클레임 활성화 (C-6) | 지급 실행 차단(운영 사고 대응). **부채를 없애지 않는다** |

> **되돌림(reversibility) 요구 — M-4의 v2판.** 밴드 상품 제공을 중단하면, 잔여 노출은
> **최장 잔여 약정일수 이내에 자연 소멸**한다. 따라서 **밴드 상품의 최초 출시는 단기 약정으로
> 제한한다**(§13 H-1 권고: 최초 ≤ 30일). 이는 밴드 모델이 04 모델보다 약한 유일한 지점
> (이미 체결된 계약은 즉시 끌 수 없다)을 **시간으로 상한**하는 장치다.
>
> `docs/patterns/pm.md`("Reversibility is a product requirement")에 따라, 이것은 구현 중
> 트레이드 대상이 아니다.

---

## 8. 데이터 모델 요구 (개념 수준)

**스키마 확정은 `prisma-db-expert` 소관이다.** 아래는 PM이 못 박는 **의미 요구**이며 컬럼명·타입은
제안이다.

### 8.1 상품 (`StakingProduct` 확장)

| 요구 | 내용 |
|------|------|
| P-1 | 기준 일이율과 **최대 가산율**을 별도로 보유한다 (예: `baseDailyRatePct`, `maxBonusPctOfBase`) |
| P-2 | `maxBonusPctOfBase` 기본값은 **"0"**. 마이그레이션이 적용되는 순간 밴드가 열리는 일이 없어야 한다 |
| P-3 | `maxBonusPctOfBase > 0`이면 `minAmount` non-null 강제 (L-5) |
| P-4 | `maxBonusPctOfBase ≤ GAME_BONUS_MAX_PCT` 검증 (L-1) |

### 8.2 포지션 (`StakePosition` 확장)

| 요구 | 내용 |
|------|------|
| Q-1 | 체결 시 `baseDailyRatePct`, `maxBonusPctOfBase`, `termDays` 스냅샷 (밴드 불변성 A1') |
| Q-2 | `fundingSource` 명시 필드 (G-A) |
| Q-3 | 원장 누계는 지급을 함의하지 않는 이름 + `SUM(payout.amount)` 누적으로만 유지 (§5.3, §5.4) |
| Q-4 | 실제 허브 이체 완료 누계 필드를 별도로 둔다. **원장 누계의 델타로 유도 금지** |
| Q-5 | 기존 포지션 백필: `baseDailyRatePct = dailyRatePct`, `maxBonusPctOfBase = "0"`, `fundingSource = grantedByAdminId != null ? PLATFORM_GRANT : USER_HUB` |
| **Q-6 (개정 01)** | 클레임 수수료를 0이 아닌 값으로 운영하기로 결정한 경우, **체결 시점의 수수료 정책을 포지션에 스냅샷**한다(§8.7 T-6). 결정 전까지는 컬럼을 만들지 않는다 — 기본값 0인 컬럼을 미리 두면 "나중에 켜면 된다"가 되어 A1′ 위반 경로가 열린다 |

### 8.3 원장 (`StakingPayout` 확장 — 신규 테이블 만들지 않음)

| 요구 | 내용 |
|------|------|
| R-1 | `amount`(총액) 유지 + `baseAmount`, `bonusAmount` 추가. `amount == baseAmount + bonusAmount` 불변식 |
| R-2 | `mpSnapshot`, `bonusPctSnapshot` 행 단위 기록. **과거 행 재계산 금지**(04 §4.3 승계) |
| R-3 | `@@unique([positionId, dayIndex])` 유지 — 멱등성의 전부다 |
| R-4 | 백필: 기존 행 `baseAmount = amount`, `bonusAmount = "0"` (금액 불변) |

### 8.4 클레임 감사 테이블 (신규)

기존 PRD R-8 승계: `userId`, `coin`, `amount`, `idempotencyKey`, `status`
(PENDING/PROCESSING/SUCCEEDED/FAILED), `hubRef`, `lastError`, `createdAt`.
`WithdrawalRequest`(`schema.prisma:120-142`) 패턴을 그대로 따른다.

> **개정 01 추가 (C-10).** 수동 대사를 가능하게 하려면 위 필드만으로 부족하다. **전송 페이로드
> 요약**과 **허브 응답 원문(또는 오류)** 을 보존하는 필드가 필요하다. 단 `NIA_API_SECRET`·서명
> 헤더는 어떤 형태로도 저장하지 않는다(CLAUDE.md 규칙 4). `status`에 **`PARTIAL`을 추가하지
> 않는다**(C-11).

### 8.5 예약 풀 (신규 — L-4)

| 요구 | 내용 |
|------|------|
| S-1 | 코인별 예약 풀 용량(설정) + 현재 예약 잔량(집계 또는 물리 행) |
| S-2 | 체결 트랜잭션 내에서 **원자적** 예약 차감. 용량 부족 시 체결 거부 (AB-17) |
| S-3 | 포지션별 예약액(`reservedBonus`)을 포지션 행에 기록해 만기 시 미사용분 반환을 계산 가능하게 한다 |
| S-4 | 예약은 **회계 장치이지 사용자 자금이 아니다.** 사용자에게 노출하지 않는다(관리자 전용) |

### 8.6 마이그레이션 규칙

- `prisma migrate dev`/`deploy`로만. **`prisma db push` 절대 금지** (CLAUDE.md 규칙 7).
- 모든 신규 필드의 기본값은 **"꺼짐"/0**이어야 한다. 마이그레이션 적용만으로 어떤 부채도
  발생하지 않아야 한다.
- 금액 컬럼을 변경하는 마이그레이션은 없다. 백필은 전부 **메타데이터**다(§5.2).

### 8.7 이체 수수료·최소/최대 단위의 관리자 설정 (개정 01 — Q5)

마스터 요구: *"이체 수수료 최소 이체 단위는 admin 에서 설정할 수 있어야 한다."*
이는 파트너 확인 항목이 아니라 **제품 요구사항**이므로 여기에 편입한다(§2.1.4).

전제 사실(N-12): 현재 출금 수수료·최소 출금 단위는 **허브가 공급**하며(`withdrawFee` /
`minWithdrawAmount`), 관리자 추가 커스텀 코인(`ManagedCoin`)에는 해당 필드가 **아예 없고**
UI에서 `'0'`으로 하드코딩된다. 즉 요구는 실재하는 공백을 겨냥한다.

| ID | 요구 |
|----|------|
| **T-1** | **코인(필요 시 코인×네트워크) 단위**로 다음을 관리자가 설정할 수 있어야 한다: ① 최소 클레임 금액 ② **1회 최대 클레임 금액**(C-11) ③ 최소 출금 금액 ④ 플랫폼 수수료. `PlatformSetting`은 `@id @default("singleton")` 단일 행이라 코인별 값을 담을 수 없으므로(schema.prisma:67-68) **별도 코인별 설정 테이블**이 필요하다 |
| **T-2** | **허브 값을 대체하지 않는다 — 가산·상향만 가능하다.** `유효최소 = max(허브최소, 관리자최소)`, `유효수수료 = 허브수수료 + 관리자수수료`. 관리자 설정으로 허브 값보다 **낮은** 최소/수수료를 만드는 것은 **금지**한다. 허브의 `withdrawFee`는 온체인 실비이며, 그보다 낮게 받으면 차액을 플랫폼이 조용히 부담하거나 전송이 실패한다 |
| **T-3** | 모든 값의 기본값은 **미설정/0**이다. 마이그레이션 적용만으로 사용자 부담이 생기거나 클레임이 막혀서는 안 된다(§8.6 승계) |
| **T-4** | 모든 값은 canonical decimal 문자열. `Number()`/`parseFloat()` 금지(CLAUDE.md 규칙 2) |
| **T-5** | 변경은 **`AuditLog`에 기록**된다(누가·언제·이전값→새값). 수수료·최소 단위는 사용자 수령액을 바꾸는 값이므로, 변경 이력이 없으면 분쟁 시 사실관계를 재구성할 수 없다 |
| **T-6** | **클레임 수수료는 계약 조건이다.** 계약된 이자를 수령하는 데 수수료를 부과하면 실질 수령 이율이 내려가며, 이는 A1′(체결 후 밴드 불변)의 위반이다. 따라서 ⓐ **기본값은 0**, ⓑ 0이 아닌 값을 도입하려면 **체결 시점 공시 + 포지션 스냅샷**(Q-6), ⓒ **이미 체결된 포지션에 소급 적용 금지**(AB-19). 이 세 조건을 만족하지 못하면 클레임 수수료를 **도입하지 않는다** |
| **T-7** | **1회 최대 클레임 금액**은 C-11의 수동 대사 규모를 유한하게 만드는 장치다. 상한에 걸릴 때 **조용히 절단하지 않고** 사용자에게 명시적으로 알리고 분할 클레임을 안내한다(§11 R-U29) |
| **T-8** | 최소 클레임 금액은 **먼지 이체 방지 장치이지 몰수 장치가 아니다.** 최소 금액 미만이라 클레임할 수 없는 잔액은 원장에 그대로 남으며, 만기·해지 등 어떤 사유로도 **소멸시키지 않는다.** UI는 "지금은 수령할 수 없음"으로 표시하되 "없어진 돈"으로 보이게 해서는 안 된다 |

> **T-2와 T-6가 이 절의 핵심이다.** 요구는 "설정 가능하게 해 달라"였지만, 설정 가능한 값이
> **사용자의 계약 수령액을 낮출 수 있는 방향**으로 열려 있으면 그것은 A1′를 우회하는 뒷문이 된다.
> 관리자 패널은 만들되, **아래로 여는 방향에는 잠금장치를 함께 만든다.**

---

## 9. 게이트 재정의 — G-1/G-2/G-3 → v2

`deep-core-00-overview-and-gate.md` §6.2의 세 조건을 **재구축 기준으로 다시 쓴다.**
아래가 §6.2를 대체한다. Phase 0 승인(§6.7)은 **변경 없이 유효**하다.

### G-1′ — 지급 레일 실증 (최우선)

> **해제 조건:** ① §4의 클레임 레일이 프로덕션에 가동되고, ② **밴드폭 0인 포지션(= 기존
> 계약과 동일한 순수 기준 이자)의 클레임이 프로덕션에서 최소 1건 이상 성공**하며,
> ③ §4.3 C-9의 불변식이 위반 없이 성립할 것.

원 G-1의 핵심 원칙("게임 보너스가 그 경로의 첫 사용자가 되어서는 안 된다")은 **밴드 모델에서도
그대로 유지된다.** 형태만 바뀐다 — v2에서는 "별도 보너스 레일"이 없으므로, 대신
**"밴드폭이 0인 포지션의 지급이 먼저 성공해야 한다"** 로 표현된다.

**즉 밴드폭 > 0인 상품은 G-1′ 해제 이후에만 개설할 수 있다.**

### G-2′ — 관할 판정 (범위 확대)

원 G-2는 "게임 메커닉의 관할별 규제 취급"이었다. **밴드 모델에서는 범위가 넓어진다.**
이제 쟁점은 게임 기능이 아니라 **금융 상품의 조건 공시**다:

- (i) "참여도에 따라 변동하는 수익 밴드"가 스테이킹 상품의 규제상 성격을 바꾸는가
- (ii) "최대 0.110%"류 밴드 상단 표기의 광고·유인 규제
- (iii) 밴드의 변동 요인이 게임 진행도라는 사실의 공시 의무 수준

그리고 **`User.locale`은 관할 통제 수단이 될 수 없다**(00 §6.4)는 판정은 그대로 유효하다.
현재 집행 가능한 통제는 전역 스위치뿐이므로, **6개 관할 중 한 곳이라도 불가 판정이면 전 관할
미출시**가 유일한 선택지다.

> **G-2′는 04 모델보다 더 엄격하게 적용되어야 한다.** 04 모델에서는 문제가 생기면 보너스를
> 장래효로 끌 수 있었다. 밴드 모델에서는 **이미 체결된 계약을 되돌릴 수 없다.**
> 따라서 **첫 밴드 포지션이 체결되기 전에** G-2′가 해소되어야 한다. §7.5의 단기 약정 제한이
> 이 리스크의 시간 상한이다.

### G-3′ — 예약 풀 재원 (질문의 형태가 바뀜)

원 G-3은 "코인별 월 예산의 실제 숫자"였다. v2에서는:

> **해제 조건:** ① 예약 풀의 코인별 초기 용량(실제 숫자)과 그 회계 항목이 확정되고,
> ② 그 자금이 **실제로 조달 가능함**이 재무에서 확인되며, ③ 기존 미청구 부채(§1.1) 전액에
> 대한 선충당이 함께 확인될 것.

③이 붙는 이유: 예약 풀만 채우고 기존 부채를 방치하면, 신규 밴드 사용자는 받고 기존 사용자는
못 받는 상태가 된다. **그것은 지금보다 나쁘다.**

### G-0″ (개정 01 — 원 G-0′ 대체, 실질 최우선) — 잔고 권위 확정 + 크레딧 수단 확인

원 G-0′은 "Nia-Hub 크레딧 수단 확인" 하나의 층이었다. 마스터 답변(§2.1) 반영 결과 이것이
**두 층**임이 드러났다. 아래 순서를 지킨다 — ①을 건너뛰고 ②부터 물으면, 답을 받아도 그 답이
설계에 어떻게 들어가는지 결정할 수 없다.

> **해제 조건 (① → ⑤ 순서):**
>
> ① **B-0 확정 — 잔고 권위(balance authority) 결정.** 사용자 출금 가능 잔고의 진실이
>    **(A) Nia-Hub**인가 **(B) BANA DB**인가를 마스터가 명시적으로 결정할 것.
>    현행 코드는 (A)이며 BANA에는 잔고 원장 자체가 존재하지 않는다(N-9).
>    **이것은 파트너 질의가 아니라 내부 아키텍처 결정이다.**
>
> ② **(B)를 택한 경우 — G-0″는 해제되지 않고 v2가 재작성 대상이 된다.** (B)는 커스터디 모델
>    변경이며 §15의 명시적 비목표다. 별도 PRD + `wallet-security-expert` 리뷰 + 재무 승인을
>    요구하고, `available = niaBal − locked`(N-4)와 스테이크 체결 경로(N-11)가 모두 무효가 된다.
>    또한 **"허브가 엔드유저 잔고를 초과하는 출금을 실행해 주는가"** 라는 새 파트너 질의가 생긴다.
>
> ③ **(A)를 택한 경우 — B-1′ 해소.** 허브에 **운영자/트레저리 → 엔드유저 크레딧** 수단이
>    존재함이 파트너로부터 확인되고, ⓐ 엔드포인트 경로 ⓑ 두 HMAC 스킴 중 어느 쪽인지
>    ⓒ 멱등키 지원 여부가 문서화될 것. (**출금 API는 이 조건을 만족하지 않는다** — 차변이다, N-10.)
>
> ④ **B-2-i 실증.** 프로덕션에서 `GET /api/v1/wallets?currency=BANA`가 실제 잔고 행을 반환하고,
>    **사용자 주도 스테이킹(그랜트가 아닌)** 이 성공한 이력이 존재할 것. N-11에 의해 이 둘은
>    동치에 가깝다. 실패하면 부채 사이징(B-5)과 그랜트 문제(B-4)의 전제가 바뀐다.
>
> ⑤ **B-2-ii 확정.** 지급 재원이 되는 계정이 **허브 측 계정**인지 **BANA 보유 온체인 지갑**인지
>    확정될 것. 후자라면 이 시스템은 그 지갑에 프로그래밍적으로 도달할 수단이 **전혀 없으며**
>    (N-13 — 서명/전송 코드 0건), §4의 자동 클레임 레일이 성립하지 않는다.

**현재 상태(2026-08-10): G-0″ 미해제.** 마스터의 답변은 ①에 대한 *의도*를 보여주었으나 결정으로
확정되지 않았고, ③④⑤는 여전히 미확인이다.

### 게이트 요약

```
B-0 (잔고 권위 — 마스터 결정)
      │
      ├─(B) BANA DB 권위 ──▶ v2 재작성 · 별도 커스터디 PRD (§15 비목표)
      │
      └─(A) 허브 권위 ──▶ G-0″ (B-1′ 크레딧 수단 · B-2-i 자산등록 · B-2-ii 계정성격) ─┐
                                                                                      ├─▶ G-1′ (밴드폭 0 클레임 프로덕션 성공)
Track 1 (허위 표시 정정) ─────────────────────────────────────────────────────────────┘         │
                                                                                                ├─▶ 밴드폭 > 0 상품 개설 허용
G-2′ (관할 — 체결 전 필수)    ──────────────────────────────────────────────────────────────────┤
G-3′ (예약 풀 + 기존 부채 선충당) ──────────────────────────────────────────────────────────────┘
```

---

## 10. 고지 문구에 대한 판정 (04 §8.1)

밴드 모델 채택은 **04 §8.1의 상시 고지 6종 중 절반을 무효화한다.** 이것은 문서 정리 문제가
아니라 **사실과 다른 고지를 렌더하는 문제**이므로 여기서 못 박는다.

| 04 키 | 원 문구 요지 | v2 판정 |
|-------|-------------|---------|
| `game.bonus.disclosureRate` | "채굴력은 게임 내 진행도이며, 체결된 일일 이자율과 약정 기간을 변경하지 않습니다" | **재작성 필수.** v2에서는 채굴력이 실제 일일 수령액을 변경한다. 새 취지: *"체결 시 확정된 기준 이율과 최대 가산율은 변경되지 않습니다. 진행도는 그 범위 안에서만 적용됩니다."* |
| `game.bonus.disclosureSeparate` | "게임 보너스는 계약 이자와 별도로 지급되는 부가 지급분입니다" | **폐기.** v2에서는 별도 지급분이 아니라 계약 이자의 일부다. 이 문구를 남기면 거짓이 된다 |
| `game.bonus.disclosureCap` | "최대 {maxPct}%로 제한되며, 일일 한도가 적용됩니다" | **재작성.** "일일 한도" 부분 삭제(§7.1 — 지급 절단 폐지). 전역 상수가 아니라 **해당 포지션의 계약 상한**을 표시 |
| `game.bonus.disclosureProspective` | "프로그램은 사전 고지 후 변경·중단될 수 있습니다" | **재작성 필수.** v2에서는 **이미 체결된 포지션의 밴드는 중단되지 않는다.** 새 취지: *"신규 상품의 제공은 중단될 수 있으나, 이미 체결된 포지션의 조건은 만기까지 유지됩니다."* — 이것은 사용자에게 **더 강한** 약속이다 |
| `game.bonus.disclosureNoLoss` | "게임을 이용하지 않아도 계약된 이자는 전액 그대로 지급됩니다" | **유효 — 그리고 v2에서 가장 중요한 문구가 된다.** 밴드 하한이 계약상 보장이라는 사실의 사용자 표현이다. 접기 안에 숨기는 것 금지 |
| `game.bonus.disclosureNoValue` | "코어 크레딧·샐비지는 게임 내 재화이며 현금 가치가 없습니다" | **유효, 변경 없음** |
| `game.bonus.dailyCapReached` | "오늘 게임 보너스 한도에 도달했습니다" | **폐기.** 지급 절단이 없으므로 이 상태가 존재하지 않는다 |
| `game.bonus.programPaused` | "게임 보너스 지급이 현재 중단되어 있습니다" | **재작성.** 지급이 아니라 **신규 상품 제공** 중단을 뜻하도록 |
| `game.mp.cosmeticOnly` | "채굴력은 진행도 표시이며 지급액에 영향을 주지 않습니다" | **밴드 상품 출시 시 렌더 중단.** 상호 배타 렌더를 테스트로 잠근다(05 AC-S7 승계) |
| `game.bonus.pendingEffect` | "다음 정산일부터 적용됩니다" | **유효, 변경 없음** (A9 승계) |

> **새 고지 1종 신설 요구 (v2).** 밴드 상품의 **체결 확인 다이얼로그**에 다음 취지의 문구가
> 반드시 있어야 한다: *"이 상품의 일일 이율은 {base}%이며, DEEP CORE 진행도에 따라 최대
> {max}%까지 가산됩니다. {base}%는 진행도와 무관하게 보장됩니다."*
> 이것은 게임 고지가 아니라 **계약 조건 공시**이므로, 게임 HUD가 아니라 **체결 흐름 안**에
> 있어야 한다. 게임을 숨긴 사용자(`game.pref.hide`)에게도 반드시 보여야 한다.

> **새 고지 1종 추가 (개정 01 — 조건부).** §8.7 T-6에 따라 클레임 수수료를 0이 아닌 값으로
> 운영하기로 **결정한 경우에만**, 체결 확인 다이얼로그에 수수료 조건이 함께 공시되어야 한다.
> 취지: *"수령(클레임) 시 {fee}의 수수료가 적용됩니다. 이 조건은 체결 시점에 고정되며 이후
> 변경되지 않습니다."* 수수료가 0이면 이 문구를 **렌더하지 않는다**(존재하지 않는 비용을
> 암시하지 않는다). 결정 전까지는 이 문자열 키를 만들지 않는다.

**원문 카피 확정은 `game-planner`(게임 표면) + `product-planner`(체결 흐름·계약 공시) 공동
소관이다.** 6개 로케일 전부 반영은 구현 담당 엔지니어.

---

## 11. 화면·기능 요구사항 개요

> **상세 화면·플로우·에러 문구는 `product-planner`, 비주얼·토큰은 `ui-ux-designer` 소관이다.**
> 아래는 PM이 못 박는 **요구사항과 금지사항**이다. 레이아웃 픽셀·컴포넌트 구조는 지정하지 않는다.

기존 `Staking.tsx`의 전통적 레이아웃(Rewards Earned 카드 / Earn 상품 목록 / My Stakes)은
**전부 교체 가능**하다(마스터 승인). 단 **DEEP CORE Phase 0 자산(N-7)은 이미 커밋된 구현물이므로
재사용을 전제**한다.

### 11.1 정보 구조 (제안)

```
/staking
├─ 【1】 DEEP CORE 캔버스 + HUD              ← 기존 DeepCoreEmbed 재사용
├─ 【2】 리그 컨트롤 바 (Rig/Crew/Depot/기록)  ← 기존 DeepCoreControlBar 재사용
├─ 【3】 수익 요약 (실화폐 영역)              ← 신규 설계. §11.2
├─ 【4】 상품 목록 (밴드 표기)                 ← 재설계. §11.3
└─ 【5】 내 포지션 목록                        ← 재설계 + 기존 WellBadge 재사용
```

### 11.2 수익 요약 — 세 숫자를 절대 합치지 않는다

| ID | 요구 |
|----|------|
| **R-U1** | **① 미청구 수익(원장 누계 − 클레임 완료), ② 클레임 완료 누계, ③ 잠긴 원금** — 세 숫자를 **항상 별개 항목**으로 표시한다. 어떤 조합으로도 하나의 숫자로 합산 표시하지 않는다 |
| **R-U2** | ①의 라벨에 지급 완료를 함의하는 어떤 단어도 쓰지 않는다(paid/지급됨/받음). 6개 로케일 전부 |
| **R-U3** | 클레임은 이 영역의 **주 액션**이다. 클레임 가능액 0이면 비활성 상태로 표시하되 숨기지 않는다(존재 자체가 "이 돈은 아직 지갑에 없다"는 정보다) |
| **R-U4** | 클레임 상태(진행 중/성공/실패)를 사용자에게 명시한다. 실패 시 "수동 검토 중"임을 알리고 재시도 버튼을 노출하지 않는다(C-4 — 중복 지급 방지) |
| **R-U5** | **클레임에 어떤 게임화 요소도 붙이지 않는다**(C-7). 연출·축하·스트릭·보너스 금지 |
| **R-U6** | **일별 내역에서 기준 이자분과 가산분을 분해해 보여준다.** 요약은 합산이어도 되지만, 내역은 반드시 분해되어야 한다 (04 M-3의 v2판) |
| **R-U7** | **미확정 수익을 추정 표시하지 않는다.** 초 단위 증가 카운터·"예상 총 수익" 금지 (§5.5) |
| **R-U8** | 수익 내역은 **실화폐 영역**에 둔다. 게임 「기록」 시트(CC/SV 내역) 안에 넣지 않는다 (05 T-6 승계 — 게임 재화와 실제 코인을 같은 목록에서 섞지 않는다) |

### 11.3 상품 목록 — 밴드가 상품의 1급 속성

| ID | 요구 |
|----|------|
| **R-U9** | 밴드 상품은 **기준 이율과 최대 이율을 함께** 표기한다. 최대치만 크게 쓰고 기준치를 작게 쓰는 표기는 **금지**한다 (유인 규제 프레임 회피 — G-2′) |
| **R-U10** | 밴드 상품과 비밴드 상품이 **시각적으로 구분**되어야 한다 |
| **R-U11** | 사용자의 **현재 진행도에서 실제로 적용될 이율**을 함께 보여준다. "최대 0.110%"만 보여주고 실제로는 0.100%를 받는 상황은 오인 유발이다 |
| **R-U12** | 예약 풀 소진(L-4)·노출 상한(L-3)으로 체결 불가일 때 **명시적으로 그렇게 표시**한다. 조용히 밴드폭 0으로 체결시키는 것 금지(§7.2) |
| **R-U13** | 체결 확인 다이얼로그에 §10의 신설 계약 공시 문구가 있어야 하며, `game.pref.hide` 사용자에게도 표시된다 |
| **R-U27 (개정 01)** | **클레임 수수료가 0이 아닌 경우에만**, 체결 확인 다이얼로그에 §10의 조건부 수수료 공시가 함께 표시된다(T-6). 수수료 0이면 렌더하지 않는다. 수수료를 **상품 목록의 작은 각주로만** 표시하는 것은 금지한다 — 수령액을 바꾸는 값이므로 체결 흐름 안에 있어야 한다 |

### 11.4 포지션 목록

| ID | 요구 |
|----|------|
| **R-U14** | 각 포지션의 **계약 밴드**(기준/최대)와 **현재 적용 이율**을 표시한다. 체결 스냅샷이므로 상품 목록의 현재 값과 다를 수 있고, 다를 때 그 사실이 보여야 한다 |
| **R-U15** | 기존 시추정 배지(WellBadge)와 캔버스 ↔ 목록 상호 이동은 **그대로 유지**한다 (05 §2.6) |
| **R-U16** | 포지션 행을 그 외 방식으로 게임화하지 않는다. 등급·별점·랭크 없음 (05 §2.6 승계) |
| **R-U17** | 자동 갱신 승계 포지션에 축하 연출 금지 (05 R-3 승계) |

### 11.5 게임 표면 (기존 자산 활용)

| ID | 요구 |
|----|------|
| **R-U18** | HUD의 채굴력 옆 표기는 **밴드 출시 전** `game.mp.cosmeticOnly`, **출시 후** 실제 적용 가산율. 두 문구는 상호 배타(05 AC-S7) |
| **R-U19** | `game.pref.hide`(완전 숨김)는 **필수 유지**. 숨겨도 XP·CC·가산분 적립에 영향 없음 (05 §6.7). 밴드 모델에서 이 원칙은 **계약 요구사항**이 된다 — 게임을 숨긴 것이 계약된 수익을 줄이면 그것은 강요다 |
| **R-U20** | 게임 실패는 페이지 실패가 아니다 (05 R-5 승계). 캔버스가 죽어도 클레임·체결·출금은 정상 동작해야 한다 |
| **R-U21** | S-2(정산 워커 중단) 상태 표시 유지. 산출이 멈춘 것을 계산값으로 대체해 "도는 것처럼" 보이게 하는 것 금지 (05 §4.2) |
| **R-U22** | 게임 코드는 `web/src/components/staking/deep-core/` 한 트리에 계속 격리 (05 G-3/AC-S8) |

### 11.6 관리자 화면

| ID | 요구 |
|----|------|
| **R-U23** | 부채 대시보드: ① 활성 원금 ② 원장 누계(미청구) ③ 클레임 완료 누계 ④ **예약 풀 용량/사용률**. 코인별·전체 |
| **R-U24** | 클레임 감사 목록(상태·멱등키·허브 참조·오류). FAILED는 **0이어야 하며 1건이라도 발생하면 킬 스위치로 차단 후 원인 규명** |
| **R-U25** | `"totalPaid"` 별칭과 "interest actually paid to date" 주석 정정 (기존 Track 1 R-5 승계) |
| **R-U26** | 그랜트 포지션이 목록에서 `fundingSource`로 명확히 구분되어 보일 것 (§6) |
| **R-U28 (개정 01 — Q5)** | **수수료·한도 설정 화면**: 코인(×네트워크) 단위로 최소 클레임 / 1회 최대 클레임 / 최소 출금 / 플랫폼 수수료를 설정한다(T-1). 화면은 **허브가 공급하는 값과 관리자 설정값과 최종 적용값(유효값)을 3열로 나란히** 보여야 한다 — 관리자가 "내가 설정한 값이 그대로 적용된다"고 오해하면 T-2의 가산 규칙이 사고를 만든다. 허브 값보다 낮은 값 입력은 **입력 단계에서 거부**한다 |
| **R-U29 (개정 01)** | 1회 최대 클레임 상한에 걸린 클레임은 사용자 화면에서 **명시적으로 안내**된다(T-7). "일부만 수령되었습니다"류의 사후 통지가 아니라, **클레임 실행 전에** 분할이 필요함을 알려야 한다 |
| **R-U30 (개정 01)** | 수수료·한도 변경은 `AuditLog`에 남고(T-5), 관리자 화면에서 **변경 이력을 조회**할 수 있어야 한다 |

---

## 12. 검증 가능성 (`pm` 패턴: 지표는 측정 가능해야 성립한다)

| 지표/불변식 | 계산 가능? | 출처 |
|-------------|-----------|------|
| 미충당 부채 총액 | 가능 | R-U23 (Track 1 R-3의 확장) |
| `amount == baseAmount + bonusAmount` (전 행) | 가능 | 원장 검증 쿼리 |
| `bonusAmount ≤ baseAmount × maxBonusPctOfBase/100` (전 행) | 가능 | 원장 + 포지션 조인 |
| 이중 지급 0건 | 가능 | C-9 불변식 |
| 그랜트가 출금 가능액을 줄이지 않음 | 가능 | G-C 불변식 테스트 |
| 예약 풀 잔량 ≥ 0 (항상) | 가능 | S-1/S-2 |
| 실사용 가산분 / 예약 가산분 (충당 효율) | 가능 | 포지션 `reservedBonus` vs 원장 `SUM(bonusAmount)` |
| 미청구 잔액 | 가능 | 원장 누계 − 클레임 완료 |
| 클레임 성공률 / FAILED 발생 | 가능 | 클레임 감사 테이블 |
| 밴드 상품 체결 거부율(L-3/L-4 사유별) | 가능 | 체결 라우트 거부 로그 — **단 거부는 행을 만들지 않고 400을 반환할 수 있다.** `docs/patterns/pm.md`("가드레일이 측정 가능한지 판정 시점에 확인한다")에 따라 **거부 이벤트를 반드시 기록 가능한 형태로 설계할 것**을 요구사항으로 못 박는다 |
| **부분 성공 발생 여부 (개정 01)** | **가능 — 단 설계로 만들어야 한다.** C-11에 따라 부분 성공은 SUCCEEDED/FAILED 어느 쪽으로도 자동 표현되지 않는다. **수동 대사 절차(C-10 런북)에서 "요청액 ≠ 실제 크레딧액" 케이스를 반드시 기록**하도록 요구한다. 기록하지 않으면 이 사건은 시스템 어디에도 흔적이 없다 |
| **관리자 수수료·한도 변경 이력 (개정 01)** | 가능 | `AuditLog` (T-5 / R-U30) |

전부 원장/감사 행에서 직접 계산되며 별도 계측 추가가 필요 없다. 익명 집계 제약
(`oil-drilling-staking-game-exposure-instrumentation-ruling.md`)의 적용 대상은 없다 — 전부
사용자 본인의 자금 거래이므로 감사 기록이 **의무**다.

---

## 13. 사람의 승인이 필요한 항목 (에이전트가 결정할 수 없음)

**아래가 해소되기 전에는 스키마 마이그레이션도, 정산 로직 변경도, 어떤 코드 착수도 금지한다.**

**개정 01에서 재구성했다.** 마스터의 답변(§2.1)이 어떤 항목을 닫았고 어떤 항목을 열었는지
"반영 상태" 열에 명시한다. **답변이 있었다는 사실 자체는 해소가 아니다.**

| ID | 항목 | 성격 | 마스터 답변 반영 상태 (2026-08-10) | 무엇을 차단하는가 |
|----|------|------|-----------------------------------|------------------|
| **B-0** *(신규)* | **사용자 출금 가능 잔고의 권위는 (A) Nia-Hub인가 (B) BANA DB인가?** | **내부 아키텍처 결정 — 파트너 질의 아님** | **미결정.** Q1 답변("DB상으로 증가만 한다")은 (B)를 시사하나, 현행 코드는 (A)이며 BANA에 잔고 원장이 아예 없다(N-9). **PM 권고: (A)** | **모든 것.** (B)면 v2 전면 재작성 + 커스터디 PRD 신설 |
| **B-1′** | (B-0 = A인 경우) 허브에 **운영자/트레저리 → 엔드유저 크레딧** 수단이 있는가? 엔드포인트·서명 스킴·멱등키 | 파트너 확인 | **미해소.** Q1 답변은 **차변(출금) 경로**를 서술했다 — 크레딧이 아니다(N-10, §2.1.1) | 레일 전체 (G-0″) |
| **B-2-i** | Nia-Hub가 BANA를 실제 자산으로 취급(등록)하는가? | 파트너 확인 **+ 프로덕션 실증 가능** | **미확정.** Q3("추가하면 된다") = 아직 아님. 단 N-11에 의해 **프로덕션에서 즉시 판별 가능** — `GET /api/v1/wallets?currency=BANA` 응답 + 사용자 주도 스테이킹 성공 이력 | 레일 전체. 또한 B-4/B-5의 전제 |
| **B-2-ii** | 지급 재원 계정이 **허브 측 계정**인가, BANA 보유 **온체인 지갑**인가? | 파트너 확인 | **미해소 — 오히려 불확실성 증가.** Q4의 "admin 지갑 주소"는 온체인 주소를 시사하며, 그 경우 이 코드베이스는 도달 수단이 **전혀 없다**(N-13, §2.1.3) | 레일 전체. 온체인이면 §4 자동 클레임 레일 성립 불가 |
| **B-2b′** | 크레딧/이체 수단에 **수수료·최소 이체 단위**가 적용되는가? | 파트너 확인 (축소됨) | **부분 전환.** Q5는 사실 확인이 아니라 요구사항이었으므로 **§8.7로 편입**. 파트너에게 남는 질문은 "크레딧 수단 자체에 비용이 붙는가" 한 줄뿐 | 최소/최대 클레임 금액의 **수치** 확정 |
| ~~B-2b (status 엔드포인트)~~ | ~~실패 조회 엔드포인트 유무~~ | — | **종결.** C-10 — 존재 여부에 의존하지 않는 설계 채택(§2.1.5) | **차단 해제** |
| ~~B-2b (부분 성공)~~ | ~~partial credit 가능성~~ | — | **종결.** C-11 — `PARTIAL` 상태를 만들지 않음(§2.1.5) | **차단 해제** |
| **B-4** | 관리자 그랜트 포지션의 원금이 사용자 허브 지갑에 **실제로 입금**되는가? | 운영 확인 | **미해소.** 질의 대상이 아니었다 | §6.3 분기, 부채 사이징. **"오늘 이미 사용자 피해가 있는가"를 가르는 질문** |
| **B-5 / H-5** | 현재 라이브 포지션 수와 미충당 부채 총액(그랜트 원금 포함) | 운영 DB 집계 | **미해소.** B-2-i가 (ii)로 판명되면 "사용자 주도 포지션이 0건"일 수 있으므로 **B-2-i 실증 후에 집계**하는 것이 효율적 | 선충당 규모, 전환 리스크 사이징 |
| **B-6** | 클레임 개방 시점에 미청구 전액을 즉시 지급 가능한가? | 재무 승인 | 미해소 | 클레임 **활성화**(설계·구현은 가능) |
| **H-1** | **§3의 밴드 모델 채택 자체.** PM 권고: 승인하되 **최초 출시는 약정 ≤ 30일, `maxBonusPctOfBase`는 10.00보다 훨씬 낮은 값에서 시작** | 제품·경영 결정 | 미해소 | v2 전체 |
| **H-2** | 그랜트 원금 처리 (§6.3의 a/b). PM 권고: **a(클레임 대상 포함)**, 단 기존 고지 내용 확인 선행 | 제품·재무 결정 | 미해소 | 부채 사이징, 그랜트 정책 |
| **H-6** | 기존 사용자 사전 고지 여부·문안 — "그동안 표시가 부정확했다"를 어떻게 커뮤니케이션할 것인가 | 제품·경영·법무 | 미해소 | Track 1 배포 및 클레임 공개 |
| **H-7** *(개정 01 신규)* | **클레임 수수료를 0이 아닌 값으로 운영할 것인가?** 운영한다면 T-6의 세 조건(기본 0 / 체결 시 공시 + 스냅샷 / 소급 금지)을 전부 수용하는가? **PM 권고: 0으로 시작한다** — 계약 이자 수령에 수수료를 붙이면 실질 이율 인하이며, 밴드 모델의 "하한 보장"이라는 핵심 약속을 흐린다 | 제품·재무 결정 | 신규 | §8.7 T-6 구현 범위, Q-6 스키마 컬럼 신설 여부 |

> **해제 기록 (2026-08-11 — 마스터 결정).** 이 표에 있던 **H-3(G-2′ 법무 검토)** 와
> **H-4(G-3′ 예약 풀 재원 승인)** 는 **게이트가 해제**되어 삭제했다. 두 항목은 더 이상 승인 대기
> 항목이 아니며 **V2-BAND(밴드 상품) 착수를 막지 않는다.** 밴드 관련 사람 결정으로 남는 것은
> **H-1(밴드 모델 채택)** 뿐이다. 예약 풀의 코인별 용량 숫자는 여전히 정해야 할 **운영
> 파라미터**이지만(§16-1), **승인 게이트는 아니다.**

> **이 하네스의 어떤 에이전트도 법률 자문을 제공하지 않는다.** `researcher`의 산출물은
> 비구속 쟁점 정리까지다.

### 13.1 Nia-Hub 질의서 (개정 01 — 7문항 → 4문항으로 압축·정밀화)

`staking-payout-rail-prd.md` §5.1의 6문항 + 기존 §13.1의 7번을 **이 4문항이 대체한다.**
마스터의 답변(§2.1)으로 일부 문항이 불필요해졌고, 남은 문항은 **오해의 여지가 없도록 문장을
바꾸었다.** 특히 1번은 "크레딧 API가 있는가"라는 추상적 질문 대신, **출금 API와 혼동될 수 없는
형태**로 다시 썼다.

> **아래를 그대로 Nia-Hub 담당자에게 전달할 수 있다.**

**1. (핵심) 운영자→엔드유저 크레딧 수단**
귀사가 BANA를 위해 운영하는 브로커/트레저리 계정에서, **실제 외부 온체인 입금 없이**
**특정 엔드유저의 인출 가능 잔고만 증가**시키는 API가 있습니까?
있다면 다음을 알려주십시오.
 ① 엔드포인트 경로 · HTTP 메서드
 ② 인증/서명 방식 — 현행 두 스킴(`X-Nia-*` Trading / `X-Api-Key` Wallet) 중 어느 쪽인지
 ③ 멱등키(idempotency key) 지원 여부와 헤더/필드명
 ④ 하나의 요청에 대해 **부분 성공(partial credit)** 이 발생할 수 있는지
*(참고: `POST /api/v1/withdrawals`는 엔드유저 잔고를 **차감**하는 출금 API이므로 이 질문의
답이 될 수 없습니다. 또한 `POST /api/v1/wallets/transfer`는 확인 결과 **동일 사용자의 지갑 유형
간** 이동만 지원합니다.)*

**2. BANA 토큰의 자산 등록 상태**
Nia-Hub는 **BANA 토큰**을 자산으로 취급합니까?
`GET /api/v1/wallets?userId=<endUser>&currency=BANA` 가 **실제 잔고 행**을 반환합니까?
등록되어 있지 않다면 등록 절차와 소요 기간은 무엇입니까?
*(당사 스테이킹 체결 로직이 이 응답에 의존하므로, 미등록이면 사용자 주도 스테이킹이 구조적으로
성립하지 않습니다.)*

**3. 재원 계정의 성격**
BANA 명의의 **허브 측 계정**이 존재합니까, 아니면 BANA가 별도로 보유·서명하는 **외부 온체인
지갑**만 존재합니까?
 - **허브 측 계정이 존재하는 경우:** 그 계정의 잔고 충전 절차는 무엇이며, 1번 API의 차변(출금)
   계정이 이 계정입니까?
 - **외부 온체인 지갑만 존재하는 경우:** Nia-Hub는 그 외부 지갑에서 엔드유저 잔고로의 크레딧을
   지원하지 않는다는 뜻으로 이해하면 됩니까?

**4. 크레딧 수단의 비용·최소 단위**
1번의 수단에 **수수료**나 **최소 이체 단위**가 적용됩니까?
적용된다면 그 값을 조회할 수 있는 엔드포인트는 무엇입니까? — `GET /api/v1/markets`의
`withdrawFee` / `minWithdrawAmount`와 **같은 값입니까, 별개입니까?**

> **각 답변이 무엇을 바꾸는가 (발신 전 합의해 둘 것):**
>
> | 답변 | v2에 미치는 영향 |
> |------|------------------|
> | 1 = "있다" | G-0″ ③ 해제. §4 레일이 설계대로 진행된다 |
> | 1 = "없다" | **§4 전면 재작성.** 자체 커스터디·온체인 이체 시나리오로 이동(§4.4). Track 1의 중요도가 올라간다 |
> | 1④ = "부분 성공 가능" | 설계 변경 **없음** — C-11이 이미 이 경우를 가정하고 있다. 단 C-10 런북의 대사 항목이 필수가 된다 |
> | 2 = "미등록" | **사용자 주도 스테이킹이 성립한 적 없음**이 확정된다. B-4/B-5의 전제와 부채 사이징이 전면 재산정 |
> | 3 = "외부 온체인 지갑뿐" | §4 자동 클레임 레일 성립 불가. 지급이 이 시스템 밖의 수동 프로세스가 된다 |
> | 4 = "수수료 있음" | §8.7 T-1의 최소 클레임 금액에 **하한이 생긴다**(수수료보다 작은 클레임은 무의미). T-2의 가산 규칙이 클레임에도 적용된다 |

---

## 14. 담당자별 다음 단계

**순서가 중요하다. 위에서부터 아래로.**
**개정 01에서 1~3번을 재배치했다** — 잔고 권위(B-0) 결정이 파트너 질의보다 **앞선다.**
권위가 정해지지 않은 상태로 질의서를 보내면, 회신을 받아도 그 답을 설계에 어떻게 넣을지
결정할 수 없다.

| 순서 | 작업 | 담당 | 선행조건 |
|------|------|------|----------|
| 0 | **이 PRD(개정 01) 검토 및 승인 (특히 H-1 밴드 모델 채택)** | **사람(마스터)** | — |
| **1** | **B-0 결정 — 사용자 잔고의 권위가 (A) Nia-Hub인가 (B) BANA DB인가.** §2.1.1을 읽고 판단. **PM 권고: (A)** | **사람(마스터)** | 0 |
| **1b** | (B-0 = B인 경우) **v2 중단 · 커스터디 모델 변경 PRD 신설** | `pm` + **사람** | 1 |
| **2** | **B-2-i 실증** — 프로덕션에서 ① `GET /api/v1/wallets?currency=BANA` 응답 확인 ② **사용자 주도(그랜트 아닌) 스테이킹 성공 이력** 유무 확인. 이것으로 §2.1.2의 (i)/(ii)가 판별된다 | `web-shared-expert`(조회 설계) + **사람**(프로덕션 실행) | 1 |
| **3** | **Nia-Hub 질의서(§13.1, 4문항) 발송·회신** | **사람** | 1 (B-0 = A인 경우), 2 |
| 4 | B-4(그랜트 실입금) 운영 확인 / B-5(부채 총액) 집계 | **사람** | 2 |
| 5 | **Track 1 계속 진행** — 허위 표시 정정 + 부채 가시화 (`staking-payout-rail-prd.md` §6). **v2를 기다리지 않는다** | `product-planner` → `web-wallet-expert` / `web-admin-expert` | 0 |
| 6 | G-2′ 예비 조사 (비구속) | `researcher` | 0 |
| 7 | §11 화면 요구사항 → 상세 FRD·플로우·에러 카피 (**R-U27~R-U30 포함**) | `product-planner` | 0 |
| 8 | §11 시각 설계·디자인 토큰 | `ui-ux-designer` | 7 |
| 9 | §10 고지 문구 재작성 (게임 표면) | `game-planner` | 0 |
| 10 | §8 스키마 **설계 검토**(마이그레이션 실행 금지) + 라이브 데이터 영향 평가. **§8.7 코인별 설정 테이블 설계 포함** | `prisma-db-expert` | 0 |
| 11 | §3 수식·§7 상한의 기술적 실현 가능성 확인 (정산 파이프라인 관점) | `web-shared-expert` | 0 |
| 12 | **H-7 결정 — 클레임 수수료를 0이 아닌 값으로 운영할 것인가.** PM 권고: **0으로 시작** | **사람(마스터)** + 재무 | 0 |
| — | **── 여기까지가 파트너 회신 없이 가능한 전부 ──** | | |
| 13 | 질의서 회신 기반 레일 상세 설계 확정 (§4 갱신) | `pm` + `web-shared-expert` | 3 |
| 14 | **사람의 구현 착수 승인** (H-1/H-2/H-7 + B-6) | **사람(마스터)** | 13, 4 |
| 15 | 스키마 + 마이그레이션 (§8, §8.7 포함) | `prisma-db-expert` | 14 |
| 16 | 정산 엔진 v2 + 클레임 엔드포인트 (§3, §4) | `web-shared-expert` | 15 |
| 17 | 그랜트 락 구조 변경 (§6) | `web-shared-expert` | 15 |
| 18 | 화면 구현 (관리자 수수료·한도 화면 포함) | `web-wallet-expert` + `web-admin-expert` + `game-developer` | 15, 7, 8 |
| 19 | **C-10 수동 대사 런북 작성** — 배포 전 필수 산출물 | `pm` + `web-shared-expert` | 16 |
| 20 | **보안 리뷰 (필수)** — 자금 이동 신규 경로 + 락 완화 변경 + **수수료 설정 경로**(사용자 수령액을 바꾸는 관리자 입력) | `wallet-security-expert` | 16, 17, 18 |
| 21 | QA: 멱등성·동시성·부분실패·§12 불변식 전부 + **T-2 가산 규칙(허브값보다 낮은 설정 거부)** | `qa-lead` | 20 |
| 22 | 킬 스위치 OFF 상태로 배포 → 선충당 확인(B-6) → 클레임 활성화 | `deploy-manager` + 사람 | 21, 19 |
| 23 | **G-1′ 해제 판정** (밴드폭 0 클레임 프로덕션 성공 확인) | `pm` | 22 |
| 24 | G-2′/G-3′ 확인 후 **첫 밴드 상품 개설** | **사람** + `pm` | 23 |

---

## 15. 비목표 (Non-goals)

- **커스터디 모델 변경.** 원금은 계속 소프트 락이며 사용자의 허브 지갑에 남는다. 자체 커스터디·
  온체인 이체는 이 PRD의 범위가 아니다. **개정 01: B-0가 (B) BANA DB 권위로 결정되면 이 비목표가
  깨지며, 그 경우 v2는 진행되지 않고 별도 PRD가 선행한다(§9 G-0″ ②).**
- **기발생 이자의 소급 취소.** 하지 않는다(§1.1).
- **기존 포지션의 계약 조건 변경.** 밴드폭 0으로 승계될 뿐 조건은 그대로다(§5.1).
- **조기 해지·가속 인출·포지션 매도 엔드포인트.** 현행에 없고 v2에서도 만들지 않는다(AB-18).
- **`REFERRAL_BONUS_ENABLED` 활성화.** 별개의 사업·컴플라이언스 결정이다.
- **KYC 레벨·거주국 판정 필드 설계.** G-2′가 이를 요구하더라도, **게임/밴드 때문에 KYC 스키마를
  바꾸는 것은 꼬리가 몸통을 흔드는 순서다**(00 §6.4 승계). 별도 제품 과제로 남긴다.
- **BANA 토큰의 발행·토크노믹스 정책.** 이 PRD는 "얼마를 갚아야 하는가"를 계산 가능하게 만들 뿐
  "어떻게 조달하는가"를 결정하지 않는다.
- **자동 클레임(무인 이체).** v2 비범위(§4.2).
- **DEEP CORE Phase 0의 재설계.** 이미 커밋된 자산이며 승인 상태 유지(00 §6.7).
- **출금 승인 정책·일일 한도·화이트리스트 변경.** **개정 01 단서: §8.7 T-1의 "최소 출금 금액"
  설정은 기존 정책 값을 바꾸는 것이 아니라 코인별 하한을 추가하는 것이며, 승인 큐·일일 한도·
  화이트리스트 로직은 건드리지 않는다.**

---

## 16. 남은 미해결 질문

1. **밴드 상품의 초기 `maxBonusPctOfBase` 운영값은?** 10.00은 코드 상한이며 초기값이 아니다
   (00 M-2 승계). 예약 풀 용량이 정해져야 역산 가능하다.
2. **L-3(사용자 밴드 노출 상한)의 구체 수치와 단위**(총 원금 기준 vs 총 최대 가산 부채 기준).
   후자가 이론적으로 더 정확하나 사용자에게 설명하기 어렵다. `product-planner`와 함께 결정.
3. **예약 미사용분의 반환 시점** — 만기 시점인가, 클레임 시점인가? 만기 반환이 회계상 자연스럽지만
   클레임 전까지는 아직 지급 의무가 남아 있다. `prisma-db-expert` + 재무 판단 필요.
4. **최소·최대 클레임 금액의 구체 수치.** 정책 틀은 §8.7로 확정되었으나 **숫자는 미정**이며,
   §13.1 질의 4번(크레딧 수단의 수수료·최소 단위) 회신에 의존한다.
5. **기존 고지 확인** — 그랜트 포지션이 사용자에게 어떤 문구로 안내되었는가? H-2 결정의 전제.
6. **밴드 상품과 자동 갱신의 상호작용** — 갱신 시 승계 포지션의 밴드는 (a) 원 포지션의 스냅샷을
   승계하는가, (b) 갱신 시점 상품의 밴드를 새로 스냅샷하는가? 기존 갱신 규칙(이율 하락 시 갱신
   실패 `FAILED_RATE_LOWERED`)과의 정합을 함께 봐야 한다. **v2 구현 착수 전 필수 결정.**
   **개정 01 추가: 클레임 수수료를 도입할 경우(H-7) 갱신 포지션의 수수료 스냅샷도 같은 질문을
   받는다. (a)/(b)를 두 값에 대해 일관되게 정해야 한다.**
7. **세무·거래내역 리포트에서 가산분의 표기** (04 EQ-5). 밴드 모델에서는 계약 이자의 일부이므로
   04보다 단순해질 가능성이 크다. 세부 표기는 밴드 상품 출시 준비 시 `product-planner`와 확정한다.
8. **(개정 01 신규) B-2-i가 "미등록"으로 판명될 경우의 처리.** 사용자 주도 스테이킹이 한 번도
   성립한 적 없다면, 현존 포지션은 전부 관리자 그랜트다. 그 경우 (i) 부채의 성격이 "사용자가
   맡긴 자금에 대한 이자"가 아니라 "플랫폼이 약속한 프로모션 지급"에 가까워지고, (ii) H-2(그랜트
   원금 처리)가 부수적 질문이 아니라 **주 질문**이 된다. **§5의 전환 전략은 이 경우에도 유효하나
   (밴드폭 0 승계는 데이터 개수와 무관), 부채 사이징(B-5)과 고지 방침(H-6)은 전면 재작성 대상이다.**
9. **(개정 01 신규) B-0가 (B)로 결정될 경우의 이행 경로.** 잔고 권위를 BANA DB로 옮기려면
   "허브 잔고와 BANA 잔고를 어떻게 최초 동기화하는가", "이후 입금은 어느 쪽에 먼저 반영되는가",
   "두 값이 어긋났을 때 무엇이 이기는가"가 전부 답해져야 한다. **이 PRD는 그 답을 갖고 있지 않고,
   추정하지 않는다.**
