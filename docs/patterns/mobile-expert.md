# Pattern Library — mobile-expert

Read on demand by `mobile-expert` only, when the current task's scope overlaps an entry below. See `CLAUDE.md` § Agent Self-Update Protocol for edit rules.

(No entries yet — agent is dormant; no Flutter code exists in the codebase yet.)
