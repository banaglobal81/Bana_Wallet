# Pattern Library — unity-fx-expert

Read on demand by `unity-fx-expert` only, when the current task's scope overlaps an entry below. See `CLAUDE.md` § Agent Self-Update Protocol for edit rules.

(No entries yet.)
