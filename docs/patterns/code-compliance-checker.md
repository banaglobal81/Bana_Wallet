# Pattern Library — code-compliance-checker

Read on demand by `code-compliance-checker` only, when the current task's scope overlaps an entry below. See `CLAUDE.md` § Agent Self-Update Protocol for edit rules.

(No entries yet.)
