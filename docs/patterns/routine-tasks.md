# Pattern Library — routine-tasks

Read on demand by `routine-tasks` only, when the current task's scope overlaps an entry below. See `CLAUDE.md` § Agent Self-Update Protocol for edit rules.

(No entries yet.)
