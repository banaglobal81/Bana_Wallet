node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.pids/
.env*
!.env.example

# Next.js build output
.next/
out/

# TypeScript incremental build info
*.tsbuildinfo

# Local-only credentials notes (never commit)
*.local.md
CREDENTIALS.local*

# Test run artifacts (generated, never referenced by the harness)
test-results/
playwright-report/

# QA sign-off marker (qa-lead writes it after a passing run; enforce-agent-boundaries.sh
# consumes it on the next deploy-manager commit — see docs/architecture/harness.md)
.claude/.qa-passed
