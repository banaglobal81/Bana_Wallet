{
  "name": "bana-staking-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "trigger.mjs",
  "description": "Always-on staking-settlement worker for the BANA web app. Schedule is admin-configurable (Admin → Settings), not a Railway Cron Schedule. Zero dependencies — plain Node fetch.",
  "scripts": {
    "start": "node trigger.mjs"
  }
}
