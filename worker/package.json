{
  "name": "bana-staking-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker (cron) that triggers daily staking interest accrual on the BANA web app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
