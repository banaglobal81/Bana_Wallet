# web/docs/

Point-in-time reports only (deploy reports, one-off test/QA run summaries). Not read by any agent, not part of the harness — `CLAUDE.md`'s Docs Index and `docs/patterns/*.md` are the on-demand references agents actually consult.

Delete a report once it's been actioned. Don't let these accumulate — they were previously left here indefinitely (6 files, none ever revisited) with no lifecycle policy, which is exactly the kind of dead weight this note exists to prevent.
